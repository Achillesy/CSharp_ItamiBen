# DECISIONS — ItamiBen

⚠️ 本文件不进 git（用户 2026-09-15 定）。

护栏清单：被推翻的方案、知情接受的代价、「不要翻案」。想改一条现有行为时先查这里。

## A. 立项（2026-09-15）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| A1 | **跟 ActivityWatch 一刀两断，但保留它的数据形状**（2026-09-16 改写，原文是「前台窗口和标题自己读」，没有后半句） | 交出「客观审计」：程序没运行的时间不再有任何记录（v3 的 Principle 3 在这里不成立）。用户明确接受 | ⚠️ **别把 aw-server 接回来。**真相源只有一个——本程序自己读的前台窗口，一秒一行写进自己的 SQLite（F 组）。回来的只是**数据的形状**（一秒的 app/title + 空闲），因为那个形状本身是对的，而且 v3 那套判定和钟面就长在它上面。<br>⚠️ 原来那句「别再把 AW 接回来当可选增强」**依然有效且更要紧**：两套真相源同时开着正是 v3 的 §15.7 那个形状的事故 |
| A2 | **不做历史统计 = 不回溯、不挖历史**（用户原话：「不再做历史统计」）。⚠️ **2026-09-15 修正措辞**：初版把它写成「连总时长都不记」，跟后来定稿的设计冲突了。准确的边界是——**不做的是「回头去挖程序没运行时发生了什么」（v3 的 `Backfill`）；做的是「累加本程序自己亲眼看到的秒」**（`during.json`，见 C5） | 程序没运行的那段时间，永远是空白，谁也补不回来 | ⚠️ 别加任何形式的回填、导入、补录。一旦有了「事后补一笔」的口子，A1 交出去的那半就爬回来了，而且这次连 AW 那样的第三方证据都没有，等于纯自报 |
| A3 | **不做 CLI** | 少了一个能在终端里干跑引擎的工具 | ⚠️ v3 的 `itami` 存在的主要理由是验证跟 AW 的连接和取数；没有 AW，这个理由消失了。**但如果将来判定引擎复杂到需要干跑验证，这条可以重新讨论**——那时的理由跟 AW 无关 |
| A4 | **名字 / 身份定死**：`AssemblyName=ItamiBen`、`App.axaml` 的 `Name="ItamiBen"`、bundle id `com.achillesy.itamiben`。⚠️ **2026-09-16 理由换了** | — | 前两个**曾经**是自身豁免的依据，那个特例已经删掉（C9）。现在它们承重的是别的东西：`localizedName` 报出来的就是这个字符串，**samples.db 里存的、rules.json 里匹配的都是它**——改了不报错，只会让历史采样和用户规则对不上号；不设 `Name` 的话 macOS 报「Avalonia Application」，一个所有 Avalonia 程序共用的名字。bundle id 则绑着 macOS 的授权，改一次用户就要重新授权一次 |
| A5 | **设计文档不进 git**，仓库里只有代码和 `README.md` | clone 下来不带 CLAUDE.md / DESIGN.md / DECISIONS.md，换机器要自己拷 | — |
| A6 | **运行时数据目录跟 v3 完全隔离**（`%LOCALAPPDATA%\ItamiBen\`） | — | 共用 v3 的 `rules.json` / `settings.json`。两个程序的规则语义会各自演进，共用一份文件 = 一个文件两条读取路径，v3 的 §15.4 被这么咬过 |

## F. 观测库（2026-09-16 定，取代 ActivityWatch）

一张表、一秒一行、判定时整段读回来重建。规格见 DESIGN §9。

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| F1 | **一张 `sample` 表，主键是 UTC 秒**；app 名和标题各自去重成 `app` / `title` 两张表 | 多两次 join（7200 行的顺序扫描，可忽略） | ⚠️ 别给 `at` 另建索引或用自增 id——`INTEGER PRIMARY KEY` **就是 rowid**，表本身即索引；换成自增 id 会白白多一棵 B 树，而且「一秒一行」就不再是引擎约束了。<br>⚠️ 别不去重：实测标题 67 字节，盯十分钟 600 行不去重是 40KB |
| F2 | **`idle` 存原始秒数，不存算好的 afk 标志**；离开区间在**读取时**算，起点回溯到 `at − idle` | 每行多一个整数 | ⚠️ **这条是承重的。** AW 的门槛是 180 秒，意味着「人不在」永远是事后才知道的——跨过门槛那一刻，前面 180 行早就写成「Chrome」了。写入时就编码成 afk，那 180 秒**永远找不回来**；回头 UPDATE 那 180 行，又破了「每一秒当场定死、永不改写」。两条路都堵死，只剩「存原始值、读时算」 |
| F3 | **库里缺一行 = 那一秒没人在看**，是第三种状态 | — | ⚠️ **绝不为了「补全」往库里插假行。** 插了就把「没观测到」和「观测到空闲」搅成一件事，而这两者待遇完全不同：前者既不计入也不算跑偏（但照样吃余量），后者是 afk。重建时缺的秒**不会被喂进 `Round.Observe`**，自动落进「没采到」，环上画留白 |
| F4 | **不常驻**：按 Start 开始录，专注达成就停 | 程序没跑的时间永远空白，谁也补不回来（A2） | ⚠️ 别改成常驻守护进程——那是 AW 的形态，而它换来的「客观审计」这个项目已经在 A1 里知情交出去了。⚠️ 但**库必须跨轮持久**：v3 的 `During` 是 checkpoint 模型，第 N 轮的秒在第 N+1 轮 Start 时才入账，库一清这条链就断 |
| F5 | **不搬 v3 的 `AwMirror` / `MirrorFeed` / `Backfill`** | — | ⚠️ 不是漏搬。那一整层（245 秒镜像、预测三规则、环形缓冲、`carryForward`、`last_updated` 游标）**存在的唯一理由是 AW 的数据会事后改写**（窗口事件滞后 6~12 秒、afk 回填 3 分钟）。**SQLite 不会改写自己写过的行**，所以「每分钟把区间读回来整个重建」就是对的做法，那一层整个不需要。搬过来只会把一堆没有理由的状态带进来 |
| F6 | **`SampleStore` 是 Core 里唯一碰磁盘的地方**（对应 v3 的 `AwClient` 之于网络）；测试一律 `Open(":memory:")` | Core 多一个 NuGet 运行时依赖（`Microsoft.Data.Sqlite`） | ⚠️ 别让别的类直接开连接。⚠️ 测试别传真实路径——v3 的 I5 栽过一次：单测把用户的账本冲成了测试数据，而且悄无声息。`:memory:` 让这件事**做不到** |

## B. 核心架构（2026-09-15，依据见 DESIGN §2 的实测表）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| B1 | **app 身份和窗口标题分两条独立的路采样**：app 走零权限非阻塞的 `NSWorkspace`（永远拿得到），标题走带超时的 AX worker（尽力而为） | 两条路的时间戳会差一点 | ⚠️ **绝对不要把两者合进一个循环** —— AW 就是这么死的：标题卡住，连它已经拿到的 app 变化都吐不出来，实测哑了 402 秒。这条是整个项目存在的理由 |
| B2 | **app 变了而标题没跟上 ⇒ 当标题未知** | 切窗口后的头一两拍标题是空的 | 沿用上一次的标题"凑合一下"——那正好是 v3 把 mame 判成「学习经济学」的那个动作 |
| B3 | **AX 调用必须 `AXUIElementSetMessagingTimeout`** | — | 这是**廉价防御**，不是主线：AX 同步 IPC 进对方进程，对方真不响应时没有超时就只能干等。⚠️ 但**别夸大它**——`aw-watcher-window-macos` 确实没设这个超时，我们一度据此断定它是被全屏应用卡死的，**后来实测推翻了**（见 B5）。设它是因为便宜，不是因为见过它救命。API 实测返回 0（成功），可用 |

| B4 | **主动轮询前台窗口，绝不订阅系统通知**（2026-09-15 实测定，DESIGN §1.2） | 每秒一次调用（微秒级，可忽略） | ⚠️ **别改成「订阅 NSWorkspaceDidActivateApplicationNotification + AXObserver，省掉轮询」**——看着更优雅、更省电，实际会瞎。`aw-watcher-window-macos` 就是这么写的，实测它**整整 13 分钟把全屏 mame 认成 Terminal**，因为 mame 是从终端启动的裸可执行文件、激活时不发那个通知。同一时间我们轮询 785 次，785 次都知道那是 mame。**轮询看得见订阅看不见的东西**，这是量出来的，不是偏好 |
| B5 | **「AX 会被全屏应用卡死」这个说法是错的，别再拿它当设计依据**（2026-09-15 推翻自己） | — | ⚠️ 这条曾经是 v4 立项理由的一部分，标注过「未实证」，后来实测 mame 全屏 784/785 次 `ok`、AX 毫无问题。`SetMessagingTimeout`（B3）**仍然要设**——那是廉价防御——但**不要**为「AX 可能永久挂住」去设计复杂的 worker 隔离。教训：带「未实证」标签的推理，在验证之前不许用来支撑架构决策 |
| B6 | **标题 worker 只读 UI 线程递给它的那个句柄**，自己不碰 NSWorkspace（2026-09-16 写 `Sampler` 时定） | worker 比主钟晚一拍，句柄可能刚好失效——那就读不到，正好是 B2 想要的结果 | ⚠️ 别让后台线程自己去问「现在前台是谁」：AppKit 不保证线程安全，而这一问本来就是微秒级的，放在 UI 线程零成本。<br>⚠️ 也别为了省事把标题读回 UI 线程——那就又变成一个循环了（B1）。<br>worker 写回的快照**必须带上「这是从哪个 app 读的」**，B2 的作废判断全靠这个字段，去掉它编译照过、行为静默退化成「沿用上一次的标题」 |

## C. 判定与计时（2026-09-15 定稿，规格见 DESIGN §4）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| C1 | **macOS 走 Accessibility，不走 Screen Recording** | 要用户授权一次；ad-hoc 签名下每次重编都失效（已用自签名证书解决，DESIGN §2.2） | ⚠️ 别改投 Screen Recording。它会**周期性弹「ItamiBen 一直在录制你的屏幕」**，对一个常驻计时器是灾难。当初唯一支持它的理由是「AX 会被全屏应用卡死」，而那条已被实测推翻（B5） |
| C2 | **「读不到标题算不算专注」这个问题不存在，别再引入它** | — | ⚠️ 这是从 v3 抄来的包袱。v3 里「没有记录」是**第三方造成的第三种状态**（`AwOffline`），所以需要一条 fail-open 政策去决定怎么对待它。**v4 没有第三方**：读是进程内同步的，读不到标题，标题就是空字符串，规则自然不匹配。**「读到什么就是什么」——没有政策可选，也就没有政策要维护。** 别再加 `Unknown` 码、别再加「宽容一下」的分支 |
| C3 | ~~**不继承 v3 的 `during.json`**~~ **2026-09-16 用户主动推翻**：他直接把 v3 那份 `during.json` 拷了过来，决定**保留原来的时间** | 口径确实不同（v3 那些秒有相当部分是 AW 历史回填出来的，不是「本程序亲眼看到的」）——**用户知情接受**，理由是那些时间真实发生过，比口径纯粹更重要 | ⚠️ **仍然别写任何导入 / 迁移代码。** 这是用户手动拷一次文件的事，两边格式恰好一样（`{"goals":{"名字":{"seconds":N}}}`）所以拷了就能用。写一条自动迁移就等于承诺以后两边格式要一直兼容，而 v3 已经定版、不会再动了 |
| C4 | **两小时硬环，触底无条件结束**；120 格 = 钟面两圈 × 60，**不环绕、不归档、不塌缩**；**长度不可配置** | 一轮最长两小时，没有例外 | ⚠️ **别做成可配置**——第一次撞墙的人就会去改它，严酷性当场归零；而且它跟钟面几何是同一个数，改了钟面没法画。<br>⚠️ **别把归档加回来**（v3 H7/H8）：归档正是为了让盘面能超过两小时而存在的，有了硬环它就没有意义了，而它带来过 §15.5「归档瞬间账会跳」那类事故 |
| C5 | **落盘是一个动作，不是一条政策**：屏幕上显示的数就是要写的数，**任何受控退出都执行同一次写入**（达成 / Give up / 环触底 / 关窗 / Cmd+Q） | 崩溃则本轮丢失，没有后路 | ⚠️ 别给不同出口写不同的逻辑（「give up 要不要打折」「强制结束算不算数」）——**那种分支就是 bug 的温床，而且概念上根本不需要**：那些秒是用户真干了活，算过的不能再拿走。<br>⚠️ 别保留 v3 的 `recordedThrough` checkpoint：没有回填就不需要水位线，留着只会让人以为还能回填 |
| C6 | **达成截止线 = 第 (120 − 休息分钟) 分钟**，休息是环尾预留的一段 | 选 50 分钟就得有效率 ≥45.5%，而且程序不提醒 | ⚠️ 别改成「专注满 120 分钟才算超时，休息超出环就截断」——那会让「休息被砍掉」变成一种可能状态，而现在**这个边界情况是被设计掉的，不是被处理的**。<br>⚠️ 别因为「50 分钟太难」去放宽：难度曲线是从这条规则自然长出来的，**是用户自己选的档位**。也别往上加档，否则会出现理论上不可能完成的设定 |
| C7 | **格子存三个计数**（`focusedSeconds` + `sampledSeconds` + `awaySeconds`），不是一个判定；累计用**整数秒**，不要更细。⚠️ **2026-09-16 从两个加到三个** | 每格三个小整数 | ⚠️ 只存一个判定就分不开三种情况，而三者待遇完全不同：跑偏**画红**、人不在**画空心虚线框**（时间确实过去了，但不怪你）、没采到**什么都不画**。<br/>⚠️ **「不怪你」和「没记录」是两件事**：一开始我把「人不在」直接丢掉，结果环上看不出区别——用户 2026-09-16 指出「离开时的空白框不见了」。<br/>⚠️ **别用毫秒或更细**：采样是 1 Hz，那是**假精度** |
| C8 | **休息期间按钮不变成 Start** | — | ⚠️ 别为了「方便连着干」把 Start 放出来。但也**别反过来去拦**：用户执意 Give up 再开新轮是他的选择，尊重。它不拦你，只是不给你捷径 |
| C9 | ~~**自身豁免 = 这一秒等于没采**~~ **2026-09-16 用户拍板删掉，硬编码特例整条消失** | 盯着自己的钟面 = 跑偏，红格照画；Start / Give up / 拖窗口 / 开设置那几秒都会计红（跑偏闪烁也会跟着起来） | ⚠️ **删的理由是原来的理由是假的**：我写 C9 时引的是 v3 的 F1「自身豁免失效 → 看一眼进度就违规的死循环」，可 v3 **同一个文件的 A5 写着「自身豁免全部删除」**——F1 是 A5 作废后没跟着改的残留。我把一条**在出处已被推翻**的护栏抄进了 v4，还拿它当依据。v3 带着这个删除定版跑到今天，没有死循环，**这条推理被它自己的项目证伪了**。<br/>⚠️ 「算成专注就白嫖」那半条是真的，但它证明的是 **v3 的做法**：v3 的 rules.json 里有一个启用着的 `ItamiTimer` 组（`^ItamiTimer\.exe$\|^ItamiTimer$`）——**交给配置，不要硬编码**。想让盯钟面算专注，自己写条规则。<br/>⚠️ 实测这个特例值多少：2026-09-16 唯一跑完的那一轮（35 分钟墙上时间、2035 秒采样），ItamiBen 在前台一共 **27 秒 = 1.3%**。而豁免和判红**吃余量一模一样**（`WastedSeconds = 走过的秒 − 专注的秒`），所以它买到的全部东西就是「那 27 秒画成空白而不是红」。<br/>⚠️ **代价是少了一档**：现在只有「红」和「写条规则变绿」，`rules.json` 表达不出第三档「既不红也不绿」。要是哪天真需要，那得是一个**用户能看见的**开关，不是藏在判定第一步的硬编码 |
| C10 | **读不到 app 名 = 这一秒等于没采**（`SecondOutcome.Unread`） | 锁屏 / 睡着 / 平台层失败的秒不冤枉人，但照样吃余量 | ⚠️ **别当成 C2 反对的那种「宽容一下」**：C2 说的是**标题**读不到时别加分支（读到什么就是什么），而这里是 DESIGN §4.1 自己举的「睡了 / 程序没跑」。Windows 锁屏时 `GetForegroundWindow()` 返回 0、macOS 登录窗口没有 frontmostApplication——判成跑偏是冤枉人，判成专注是送人情 |
| C11 | **`StartedAt` 抹到整分**（沿用 v3 的 A6） | 点 Start 之前最多 59 秒没人看着：既不计入也不算跑偏，**但照样吃余量**（50 分钟档占 1.6%） | ⚠️ 别改成「从点击那一刻起算」：格子索引跟钟面刻度就对不上了，格子 0 只有几十秒，「转完两圈就是到点，**显示即规则**」当场不成立 |
| C12 | **一轮只盯一个目标，界面是单选 + 默认选中**（2026-09-16 用户澄清：**一直都是这么要求的**，跟 v3 一致） | — | ⚠️ **这条我一度读错过**：DESIGN §1 原来写的是「**勾选**允许的小目标」，我照字面做成了 CheckBox 多选，用户 2026-09-16 指出那不是他的意思。文案已改成「选一个」。<br/>⚠️ **永远有一个是选中的**：上次那个没了（rules.json 改过）就退回第一个，不让用户面对一个「什么都没选」的起点。<br/>⚠️ `Round` 的接口仍然收一个**列表**，别改成单个字符串——Core 对此无所谓，而那套「一秒只算给排在前面的那个」的判定和测试还在，真要放开多选时不用重写 |
| C14 | **界面完整保留 v3 的样子**（2026-09-16 用户定稿）。ItamiBen 只多两样，都是用户点头认可的：**钟面右键菜单**和**底部那条应用名 / 标题显示栏** | — | ⚠️ **别再往主窗口加东西。** 我加过一条「25 min to go · 90 min of slack」的读数行——v3 没有，已经撤掉：钟面本身就是读数（三段色块），再写一遍文字等于承认视觉没说清楚。<br/>⚠️ 逐项对齐的清单：外边距 `18,14,18,16`；钟面右上角**只有三个图标**（主题、喇叭、图钉），**齿轮单独在骨牌那行右端、图钉正下方**；卡片**内外间距都是 8**（`Border.Padding` 和 `StackPanel.Spacing` 取同一个值，一套节奏）；版权行是「居中版权 + 右对齐版本号」的两列 Grid；提示条在**骨牌那一格**里居中、**两层实色文字**。<br/>⚠️ 喇叭和图钉**有** ToolTip，主题和齿轮**没有**（E10：齿轮开模态框、主题重画整扇窗，两者都会把气泡留在屏幕上）。<br/>⚠️ **唯一刻意不跟 v3 的**：休息中按钮不变成「New round」（C8 禁止，不给一口气连干三轮的捷径） |
| C15 | **rules.json 的正则区分大小写，跨平台要自己写全**（2026-09-16 实测撞上） | 同一份规则要同时覆盖 `Claude` 和 `claude.exe` | ⚠️ 从 v3（Windows）搬过来的 `"app": "claude\\.exe"` 在 macOS 上**一秒都不会命中**，因为读到的是 `NSRunningApplication.localizedName`（`Claude`，无后缀）。<br/>⚠️ **这个错不报错**——症状是「选了编程，整轮全红」，而红格正是本程序的正常输出，所以它看起来像「你今天确实没干活」。2026-09-16 那一轮实测：旧规则专注 **0 秒**，改完 **192 秒**，同一批采样。<br/>⚠️ 写成 `^(Claude\|claude\.exe)$` 这种显式两边都列的形式，**别指望引擎替你忽略大小写**——`GoalRules.Compile` 只给 `Compiled \| CultureInvariant`，这是有意的：匹配语义是用户手写文件的约定，不该由程序单方面放宽 |
| C16 | **闹钟时刻只在拨针时显示，停手两秒收起**（照 v3 的 3.9.4） | 平时看不到闹钟设在几点，只能读黄针；**过期的残影和真上弦看起来一样**（v3 的 E7 接受了这个） | ⚠️ 收起的时刻**复用 `_alarmQuietUntil`**——「停手两秒后收起」跟「调整期结束」天然是同一个时刻，别再开第二个计时量。<br/>⚠️ 这个判断是**每秒**的，不能挂到分钟节拍上：那样标签会一直糊到下一个整分钟。<br/>⚠️ 我为此加过的 `AlarmClock.IsArmed` 随之没有调用方，已按 E5 的纪律删掉 |
| C15 | **状态栏两行：应用名居中、标题左对齐，两行都单行 + 省略号截断** | 长标题看不全（鼠标悬停也没有提示） | ⚠️ **绝不折行。** 窗口是 `SizeToContent="Height"`，一折行整扇窗就变高、边框跟着跳——用户 2026-09-16 点名要避开的正是这个。所以 `TextWrapping="NoWrap"` + `MaxLines=1` + `TextTrimming="CharacterEllipsis"` 三个一起，少一个都会折 |
| C13 | **人不在（键鼠空闲 ≥ 180 秒）⇒ 这一秒等于没采**，而且**压过屏幕上是什么**（2026-09-16 加，DESIGN §9.5） | 离开的秒既不计入也不算跑偏，但照样吃余量 | ⚠️ **别一行一判**。门槛是**事后**才跨过的：跨过那一刻回头看，前面 180 秒其实一直没人，可那 180 行当时记下的 idle 是 1…179，一行一判会把它们判成跑偏。回溯由 `AwayMap` 跨行算，起点回到 `at − idle + 1`。<br>⚠️ **必须压过 app 名**：2026-09-16 实测 **macOS 锁屏时前台读到的是 `loginwindow` / `Login`，不是空字符串**——不压过的话锁屏一小时就是一小时红格，正是 v3 3.9.x 那个大 bug 的形状。<br>⚠️ 门槛别调：179 秒不动**不算离开**，那是在看内容。180 跟 AW 的默认值一致 |

## H. 确认与交互（2026-09-16 从 v3 搬过来）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| H1 | **两个会作废整轮的动作各问一次**：点 Give up、专注途中关窗口 | 多一次点击 | ⚠️ **问不等于拦**（C8）：用户执意放弃是他的选择，这里只是确认他知道按下去会发生什么。别把它变成劝阻。<br/>⚠️ 关窗口那条**必须先 `e.Cancel = true` 再去 await**：关闭事件不等异步，不拦下来窗口当场就没了，问了也白问。<br/>⚠️ 确认框也要 `Topmost`：主窗口可能被钉住，普通模态窗会沉到它下面，表现为「我点了 × 什么也没发生」 |
| H2 | **点钟面上的小红圈 = 瞄一眼 12 小时内的下一条**，只留 3 秒。**纯读**——不出声、不推进水位线 | — | ⚠️ **别做成 Button**：Button 内部会把 `PointerPressed` 标成已处理，挂在钟面上的拖窗口订阅就收不到了。改成纯几何命中测试，跟拖拽**共用同一次按下事件**。<br/>⚠️ **必须在按下那一刻判，不能等松开**：`BeginMoveDrag` 只能在按下时调用。<br/>⚠️ 命中范围要给**固定像素**的容差（6px，不跟着表盘缩放）：红圈画出来半径才 7px 左右，纯几何精确点太挑手感。<br/>⚠️ 提示条的到期检查要按**秒**不按分钟——v3 那边是按分钟的，它自己的注释里记着「瞄一眼那 3 秒其实也有这个毛病，**还没修**」，这里一并修掉了 |

## G. 窗口外观（2026-09-16 从 v3 搬过来）

无边框 + 透明底 + 置顶，参数在 `layout.json`。规格见 DESIGN §11，
v3 那边的护栏在 `../ItamiTimer/DECISIONS.md` 的 K 组。

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| G1 | **不透明度用 `OpacityMask`，不用 `Opacity`** | 多一个画刷 | ⚠️ **这条是实测出来的，不是偏好。** Avalonia 的 `Visual.Opacity` 是**逐个绘制操作**各自半透，不是「整个控件先合成一层再降透」。钟面那块白画在木色**实心圆盘**之上，逐笔半透会让木色透过白面混上来——v3 的用户一眼看出「表盘泛黄」。headless 实测：`Opacity=0.5` 时白面净区是 (207,189,176,A=201)，`OpacityMask=0.5` 是 (255,255,255,A=127)。<br/>顺带的好处：控件的 `Opacity` 保持 1，**命中测试完全不受影响**（拖钟面照常） |
| G2 | **卡片拆成底色层 + 内容层**，只有底色跟着淡 | XAML 多一层 `Grid` + `Border` | ⚠️ 别给整块套透明度。挡住桌面的是那块**实心底色**，不是细细的文字；把文字一起调淡，低透明度下就没法读了。窗口一透明，按钮和文字背后可能是任何东西 |
| G3 | **拖窗口挂在钟面上，不是整扇窗口** | 卡片区域拖不动 | ⚠️ 挂到整扇窗口上会跟卡片里的按钮打架。<br/>⚠️ 实际可拖范围是**圆的不是方的**：Avalonia 对自绘控件的命中测试按真正画过的绘制操作逐个判，所以四个角（一笔没画的透明区）拖不动，而圆盘外沿再往外一点点能拖——那儿画着钟投在墙上的影子。v3 的用户看过之后认为圆形**更好**，别去 override 成方的。<br/>⚠️ `BeginMoveDrag` **只能在按下那一刻调**，等「松开算不算点击」判完就来不及了 |
| G4 | **没有标题栏之后，关窗口靠钟面上的右键菜单**（只有「置顶」和「关闭」两项） | 第一次用得自己发现 | ⚠️ 别做成一整套窗口菜单。⚠️ 关闭走 `Close()` 而不是直接退进程——那样才跟点 × 同一条路径，会走 `OnExit` 落盘 |
| G5 | **`layout.json` 用户手写、程序只读、只在启动时读一次** | 改完要重启 | ⚠️ 别搬进 `settings.json`：那个文件程序随时整份重写，手改会被下一次写盘覆盖。<br/>⚠️ 解析必须跟 `rules.json` 同一套三件套（注释 / 尾逗号 / 键名大小写）。<br/>⚠️ `opacity` 声明成 `JsonElement` 而不是 `double?`：手写 JSON 里写成 `"50"` 完全可能，声明成 `double?` 会让反序列化整个抛异常，**连 `layout` 那一档也跟着丢**。<br/>⚠️ 写错、超范围一律**强制 90，不是夹到边界**——夹到边界会让「我写了 5」和「我写了 10」看起来一样，用户以为生效了其实被悄悄改掉了 |
| G6 | **窗口位置读回来必须夹回屏幕内**，夹不住就居中 | — | ⚠️ **别在退出那一刻现读 `Position`**：2026-09-16 实测那样存下来的是 `(0,0)`，下次启动窗口被拖到屏幕左上角。改成订阅 `PositionChanged` 一有移动就记——拖窗口本来就会连续触发它，取最后一个值天然是对的。<br/>⚠️ 别直接信存下来的坐标。显示器拔掉、分辨率改了、外接屏断开之后，上次那个位置可能整个落在屏幕外——而**无边框窗口连标题栏都没有，再也找不着也够不着** |
| G7 | **右上角两个小图标全是矢量画的，仓库一张位图都没有**（图钉 = 置顶，主题 = 日/夜） | 每个形状要手写路径数据 | ⚠️ 别改回字体字形。v3 原来用 Segoe Fluent Icons，那套字体只在 Windows 预装——**macOS 上就是两个豆腐块**。改成矢量不只是为跨平台将就：钟面、指针全是代码画的，图标字体是这条规矩上唯一的例外，去掉它反而把规矩补完整了。<br/>⚠️ **光晕描边那一层不能省**：窗口无边框透明之后图标可能直接叠在任意壁纸上，只有墨色一个颜色时遇到明暗接近的区域就看不清了。<br/>⚠️ 路径里的小数**必须用不变文化格式化**——`Geometry.Parse` 只认小数点，跟着系统区域走会在用逗号做小数点的地区**安静画错** |
| G8 | **图钉图标 / 右键菜单那一项 / `settings.Pinned` / `Topmost` 是同一个状态的四种表现，只有 `SetPinned` 一个入口** | — | ⚠️ 别在任何一处单独改。分家之后**界面还是好好的，只是说的不是同一件事**——这正是最难发现的那类错。<br/>⚠️ 主题一换图标必须重画（墨色取自当前调色板），所以右键菜单那两项要**留着引用**：菜单只构造一次，不会自己更新 |
| G9 | **状态靠形状 + 不透明度双重表达，从不用文字**（图钉：填实/空心 + 0.95/0.40） | — | ⚠️ 图钉的「关」态**刻意不用 X**：满不透明度下 X 会读成「置顶被禁用了」，跟「当前没置顶」正好相反。右键菜单里那个 X 是另一回事，它就是字面意思。<br/>⚠️ 主题图标画的是**当前所处的状态，不是「点了会变成什么」**——跟图钉一致，这一排读法要统一 |
| G10 | **应用图标也是代码画的**（`TomatoIcon` + `IconExport`），打包时现导；`tomato.ico` 进仓库只因为 MSBuild 的 `ApplicationIcon` 在编译时就要有那个文件 | 仓库里躺着一个 25KB 的二进制 | ⚠️ 它是**导出产物不是美术资源**：改番茄的形状去改 `TomatoIcon.cs`、重跑一次 export-icon，别拿图形软件去 P 那个 ico。<br/>⚠️ 导出必须走 **headless builder**（`UseHeadless` + `UseSkia`，`UseHeadlessDrawing=false`），**不能用 `UsePlatformDetect`**：后者会去初始化原生窗口系统，**没有图形会话时当场崩**（`Avalonia.Native was not able to start the RenderTimer, -6661`）。而导出图标是**打包步骤的一部分**——打包不该关心有没有人登录着桌面。2026-09-16 我自己就因为屏幕锁着撞过这个错误码 |
| G12 | **图标是青番茄：整果，剪影和叶子跟 v3 一模一样，只换颜色**（2026-09-16 用户拍板） | 跟 v3 的区别只有色相一项 | ⚠️ 用意是 Dock 里一眼分清 ItamiBen 和 ItamiTimer（同机并行）。**红 vs 绿在 16px 下也一眼分得开**——我之前说「只改色相在缩略图里会糊成一团」是**说过头了**，那对两个相近的色相成立，对红绿不成立。<br/>⚠️ 果身要**闷一点的橄榄绿，不能用亮柠檬绿**（用户 2026-09-16：「太透了，像一颗葡萄」）：亮而饱和的黄绿读起来是**通透的浆果**，真正的青番茄是压着土气的、偏暗的绿。**高光也要跟着压**——又亮又大的一块高光是「葡萄感」的另一半来源。<br/>⚠️ 这里**真实感压过分辨度**，是用户明确定的：「糊也不怕，现实中绿番茄和叶子本来颜色就相近，我要的是一眼看出这是个番茄」。所以果身和萼片只拉开明度、**不硬掰色相**——我原来那条「挑成邻近的绿会糊成一团」是在用可读性换真实感，方向反了。<br/>⚠️ **深绿条纹（2026-09-16 用户加的）两头都要停在果肉里**，别一路通到果蒂和果脐：一路通到底的等距竖条会把番茄画成**南瓜**——「一瓣一瓣」那种读法全来自条纹在两极汇聚。停在中间，它才是长在皮上的花纹而不是瓣与瓣的分界。宽窄、长短、起止高度**全都要不齐，而且左右不对称**。<br/>⚠️ 条纹靠**裁剪**贴合轮廓（`PushGeometryClip` 果身自己的形状），不去算每条到边缘的交点：轮廓和条纹从此不可能对不齐。<br/>⚠️ 条纹画在**高光之前**：高光是表面反光，本来就该把底下的花纹压住；反过来条纹会像印在玻璃上一样浮起来。<br/>⚠️ **不许把参考照片切进仓库**，两个理由各自都足够：那是带水印的图库照片（图标是要随程序发布的）；而且 G10 立的规矩是**图标也是代码画的，仓库里没有位图**。<br/>⚠️ 我先画了一版**切开的剖面**（果肉 / 果汁 / 籽），改了五轮，最后用户选了整果——剖面那版在 git 的 `2b46059`，要捡回来是一次 `git show`。教训不在画得对不对，而在**改到第三轮还没问一句「要不要换个方向」**：用户一句「直接把红番茄换青番茄了事」就结束了，这句本该由我在第二轮提出来 |

## I. 单实例（2026-09-16 从 v3 搬，实现换了）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| I1 | **同一时刻只允许一个 ItamiBen**，锁是**在运行时目录里独占打开一个文件**（`singleton.lock`） | 第二次双击图标在 macOS 上没有任何反应（Windows 会把旧窗口提到前台） | ⚠️ **不要用 v3 那个不带前缀的命名 Mutex**：2026-09-16 实测它**在 macOS 上根本不生效**——.NET 把 Unix 上不带前缀的命名 Mutex 放进 `/tmp/.dotnet/shm/session<N>/`，N 是**进程的 session id**。`open` 起的 .app 落在 `session1`，终端起的落在 `session<shell pid>`，**两个命名空间谁也拦不住谁**。当场两个实例一起跑起来了，`/tmp/.dotnet/shm/` 下面真躺着两份同名的锁。<br/>⚠️ 改 `Global\` 前缀能绕过，但那是**全机器**命名空间：同机两个用户（快速用户切换）会互相挡住。**独占文件锁两头都解决**——锁文件就在本用户的运行时目录里，跟它保护的账本待在一起，天然按用户隔离，而且一份实现管两个平台。<br/>⚠️ **FileStream 要用静态字段拿住**：终结器一跑就关句柄，句柄一关锁就没了，后来的实例会误以为自己是第一个。<br/>⚠️ **只有 `IOException` 才算「已经有一个在跑」**；权限类异常一律**放行**——单实例是便利不是安全边界，让程序起来比让它因为一个锁文件起不来强。<br/>⚠️ 被挡回去的那个进程**不许调 `Log.Start`**：那是 `File.WriteAllText`，会把**正在跑的那个实例的日志整份清空**。为它专门开了 `Log.Aside`（只追加），全项目只有这一个调用方 |
| I2 | **单实例检查排在两个导出开关后面** | — | 导出跑完就退、不碰任何运行时文件，没理由因为「已经有一个在跑」被挡住——而打包的时候程序多半正开着 |
| I3 | **日志一共只留两份**：当前这一份 + 上一次的 `.old`。启动时把旧的**挪走**（不是清空），运行中超过 1MB 也挪一次 | 上上次的日志不可能再找回来 | ⚠️ **原来启动时是 `File.WriteAllText`**，等于「上一次运行的日志在下一次启动时被抹掉」——而「昨晚它自己没了，日志呢」正是最需要日志的那一刻。改成挪走，代价只是多一个文件。<br/>⚠️ **运行中也要滚**：启动时滚一次挡不住「开一整天不关」这种用法，而这程序就是要开一整天的。两条路共用同一个 `Roll`，只差一个 `force`——**别写成两套**，那种「启动滚存和运行滚存规则不一致」的 bug 极难发现。<br/>⚠️ `Log.Aside`（被单实例挡回去的那个进程写的那一行）**不滚**：它不是这个文件的主人，而且只写一行 |
| I4 | **`alarms.cron` 到点时，自绘提示条和系统通知一起出，不是二选一**；系统通知**一条事件一个、永远不合并** | 同一件事提醒两遍 | ⚠️ 两者各补对方的短板：提示条保证**屏幕上一定看得见**（系统通知可能被「请勿打扰」吞掉），但它有硬高度上限、多出来的只剩一个 `+N`；**通知中心那一份才是不丢内容的**，而且关掉程序还能翻回来。<br/>⚠️ **无条件执行**：`alarms.cron` 那个开关**只管响不响铃**（v3 的 J6），主链路每分钟照走。<br/>⚠️ Windows 那条走 PowerShell 调 WinRT，AppId **必须用 PowerShell 注册过的那串 AppUserModelID**，裸字符串 `"Windows PowerShell"` 弹不出来。v3 为此差点把整条路判死刑——后来发现早先那条**其实送达了**，只是「请勿打扰」把横幅吞了。**「没看见」不等于「没送达」**，这也正是提示条必须并存的理由。<br/>⚠️ 脚本走 `-EncodedCommand`，绕开引号地狱；里面的文字**先 XML 转义再 PowerShell 单引号转义，顺序不能反** |
| I5 | **面向用户的文档一共四份，改一份就要过一遍另外三份** | 同一件事写四遍 | 四份是：`README.md`、`README_ZH.md`、`installer/README.txt`（Windows 安装包里）、`pack-macos.sh` 里那段 `Read Me.txt`（.dmg 里）。<br/>⚠️ **后两份不在仓库显眼处，最容易被忘掉**——v3 就漏过一次（它的 L30）。它们不是 README 的副本：安装包那份要讲 `%APPDATA%`、.dmg 那份要讲 Gatekeeper 和辅助功能授权，**各自面对的是「刚装完、还没跑起来」的人**。<br/>⚠️ README 是**唯一会被外人读到的东西**，所以它用英文、中文另开一份；而 `DESIGN.md` / `DECISIONS.md` / `CLAUDE.md` 不进 git，用中文 |
| I6 | **跑偏归因只做诊断，永不回流判定**；它跟判定调**同一个 `Judgment.Judge`**、同一批行、同一张 `AwayMap` | 每分钟多一行日志 | 环上只说「这一分钟红了」，两小时后你根本想不起来当时开着什么。<br/>⚠️ **别在归因里另写一套匹配**：两边迟早分家，而分家之后日志会**理直气壮地给出错误的解释**，比没有更糟。<br/>⚠️ v3 的教训：它这行日志比归因本身还早就有，但索引取了 `cells[^1]`（指向承诺弧的投影尾），读到的永远是 0——**整个生产历史里一次都没打出来过**。而「零匹配」看起来跟「一直没跑偏」一模一样。**一条从不触发的诊断，长得和「没什么可报的」完全一样**，所以「刚过去的那一分钟」这一步被单拎成 `TimeGrid.PreviousMinute` 并测住了。<br/>⚠️ 平局取**先出现**的那扇窗口，不靠字典的枚举顺序——那不是合约，改个实现就悄悄换了答案，而「同样输入两次跑出不同日志」事后根本没法查 |
| I7 | **`--dial-specimens` 把钟面离屏渲成 PNG**，喂的是**真的 `Round`**，不手搓 `MinuteCell` | 多一个调试出口 | ⚠️ 钟面活在 App 层，Core 的测试够不到——v3 那个「承诺弧跨整点跳一整圈」的 bug 正是这么漏到线上、最后靠用户发现的。**半径、角度、图层顺序这类问题真的只能靠看图抓**。<br/>⚠️ **v3 那边是手搓 `MinuteCell` 的，v4 改成喂真 `Round`**：手搓等于把「秒数怎么变成档位」在样张里**又写一遍**，两边分家之后样张会**让错误的渲染看起来是对的**。<br/>⚠️ 要垫一层背景再存 PNG：钟面画在透明底上，半透明那张直接存出来是叠在看图器的棋盘格上的，**根本看不出它到底透了多少** |
| I8 | **`Command.RunDetached` 从 `LaunchDetached` 里拆出来，只为了能测** | 多一个公开方法 | ⚠️ `LaunchDetached` 到点会**现读 rules.json**，而这台机器上 `executeCommand.macos[0]` 就是重启命令——**测试要是调它，当场把机器重启了**。拆开之后测试只喂一条自己写的无害命令，走的却是同一条起进程的代码，**引号那段一个字节都没绕开**。<br/>⚠️ 那个测试断言的**不是「进程起来了」**：v3 那个 bug 里「调用到了」恰恰是唯一还正常的一环，拿它当断言什么都测不出来。断言的是**副作用真的发生了、而且双引号原封不动** |
| I9 | **观测归数据库，日志只留判断和失败**（2026-09-16 用户定）。删掉了「每秒一行」那条日志 | 想逐秒回看就得读 `samples.db`（为此加了 `--dump-samples`） | ⚠️ 那一行记的 `at` / `app` / `title` / `idle` **四样全是 samples.db 的列**，外加 `outcome` / `focused` / `slack` ——这三样是它们的**纯函数**，而且重放幂等（`RebuildTests` 守着）。**整行都是数据库的副本**，50 分钟一轮 3000 行、几百 KB，还把真正要看的那几行冲得找不着。<br/>⚠️ 这条的由来是 v3：那边判定数据在 AW 那边、本程序基本只读，日志**就是**唯一的现场记录，所以什么都往里写是对的。v4 自己就是记录者，**同一条理由反过来成立了**。<br/>⚠️ 删之前必须先有 `--dump-samples`：没有 SQLite 工具的机器上，「观测归数据库」会等于「观测没了」。<br/>⚠️ 每分钟那条构成行**只在这一分钟没满格时才写**——满格的分钟没有任何可解释的东西。于是日志有了一个好性质：**你做得越好，它越安静**，翻开看到的每一行都是真的发生过什么的那几分钟 |
| I10 | **「这个时刻落在哪一格」的算式只有一份，在 `Round.CellAt` 里** | — | ⚠️ 别让调用方自己减一遍：v3 那行诊断日志正是**调用方算错索引**（取了 `cells[^1]`，指向承诺弧的投影尾），读到的永远是 0，**整个生产历史一次都没打出来过**。算式只有一行，但它必须**只有一份、而且被测住**（含边界：第 0 格、最后一格、早于开始、超出环） |
| I11 | **这个程序没有运行日志。`samples.db` 就是记录**（2026-09-16 用户定）。文本文件只装**够不着数据库**的话 | 想看现场要会查一下数据库（为此有 `--query`） | ⚠️ **同一条理由，在 v3 和 v4 指向相反的结论**：v3 的判定数据在 AW 那边、本程序基本只读，日志**就是**唯一的现场记录，所以什么都往里写是对的；v4 自己就是记录者，再单开一份文本就是**第二个真相来源**，而两份真相迟早对不上。<br/>⚠️ 三条规矩按顺序判：**①能从 `sample` / `round` 推出来的，一行都不记**（每秒的 app / 标题 / 空闲是列，专注秒数、余量、每格构成、判定结果全是它们的纯函数，重放幂等有测试守着）；**②别处留不下痕迹又值得注意的**进 `event` 表（闹钟响了、提醒到点了、命令跑了什么退出码、出错了）；**③够不着数据库的**才落文本。<br/>⚠️ **`event` 表要防洪**：`Sampler` 那两个 catch 每拍都可能进来，平台层一坏就是一秒十条。数据库不像文本那样「写大了就滚存」——灌进去的垃圾会一直躺着。同一句话一分钟只记一次。<br/>⚠️ **唯一破例记「正常事件」的是每次启动那一行**：`sample` 只在专注阶段才写，采样流里到处是空档，少了它「那段时间没在跑」和「在跑但没录」**长得一模一样**——而这恰恰是回头查问题第一个要分清的。<br/>⚠️ **「重建对不上」里那个 ①（跨过离开门槛的回溯纠正）一个字都不记**：它每一轮都会发生，是设计本身。只有 ② 才值得留痕 |
| I12 | **程序自己的设置搬进 `samples.db` 的 `setting` 表**，`settings.json` 作废（2026-09-16 用户定） | 想看设置要查一下库 | ⚠️ 分界线是**谁写**，不是「重不重要」：`rules.json` / `alarms.cron` / `layout.json` **用户写、程序只读**；音色、置顶、窗口位置、闹钟时刻**程序写、用户只看**。既然用户从来不手改，就没理由为它单开一个文件。<br/>⚠️ 顺手解决了一个真问题：JSON 那套是**整份重写**，而「一次写全部」正是两个实例互相覆盖的根源（I1）。现在逐键 upsert，一个事务。<br/>⚠️ **读写复用同一个类型模型和同一个解析器**：序列化成 `JsonObject` 再把顶层摊平成行，读回来反着拼。所以 `value` 存的是 JSON 片段（字符串带引号）。别为了「好看」另写一套解码——v3 的 §15.4 就是同一份数据两条解析路径，咬了两次，症状都是半个文件安静地失效。<br/>⚠️ **`ToJsonString()` 不继承序列化的编码选项**：不显式传的话中文会变成 `\u81EA…`，而「出了问题直接查数据库」的前提就是那一眼能看懂。<br/>⚠️ `Save()` 的 store **记在实例上，不让调用方传**：它有五个调用点，只要有一处忘了传，那一处的改动就**安静地不落盘**——而「设置没保存」最容易被当成「我记错了」 |
| I13 | **累计账本搬进 `samples.db` 的 `total` 表**，`during.json` 作废；而且**只加不写**（2026-09-16 用户定） | 想看累计要查一下库 | ⚠️ 跟设置同一条分界（I12）：程序写、用户只看的东西没必要单开文件。<br/>⚠️ **但账本跟别的表不是一个量级**：观测丢了还能重新采，账本丢了就是几十上百个小时凭空消失。所以加法走 `UPDATE ... seconds + $n` 的 upsert，**在库里原子地加**——老的 JSON 是「读出来、内存里加、整份写回」，中途进程没了就把**之前所有的累计**一起带走。现在最坏只丢这一轮。<br/>⚠️ **先落库再更新内存**：反过来的话落库炸了、界面上的数字已经涨了，下次启动自己缩回去，看着像程序把时间吃了。<br/>⚠️ 迁移**先落库确认没抛、再改名**，顺序反了中途出错就两头都没了。<br/>⚠️ `GoalTotals.Parse` 留着**只为迁移这一次**，`ToJson` 删了——没有任何东西再写 JSON 了 |
| I14 | **`layout.json` 并进 `rules.json` 的顶层**（2026-09-16 用户定）：少一个要手写的文件 | 外观开关和目标规则挤在一份文件里 | ⚠️ **必须长在 `RulesFile` 上、跟着同一个解析器走**，绝不能为它另开一条 `JsonDocument` 路径——v3 的 §15.4 就是同一份文件两条读取路径、两套解析选项，靠人记得同步，咬了两次，症状都是**半个文件正常工作、另半个安静地失效**。<br/>⚠️ `opacity` 仍然声明成 `JsonElement`：手写文件里写成 `"50"`（带引号）完全可能，声明成 `double?` 会让整份反序列化当场抛——**连 `Groups` 都跟着丢**，那就成了「改了个外观开关，所有目标都不见了」。<br/>⚠️ **程序永远不写 `rules.json`**，所以这次搬家没法自动迁移。作废的 `layout.json` 还在就**记一条 warn**：它还在说明用户以为它还管用，而「改了没反应」正是这个项目最恨的那类失败。<br/>⚠️ `WindowLayout` 从此**不碰文件也不解析 JSON**，只剩「怎么解释」两个纯函数；解析的测试相应搬去 `GoalRulesTests` |
| I15 | **配置全部住进库里，`rules.json` / `alarms.cron` 作废；库改名 `ItamiBen.sqlite3`**（2026-09-16 用户定）。配置由**智能体**改，不再手写 | 想改配置就得会点 SQL，或者叫个智能体 | ⚠️ 这条的**前提是「用户不手写」**：用户说「我想要什么」，智能体去改库。于是「程序永远不写 rules.json」那条硬规矩（存在的唯一理由是保护手写的注释）连同它带来的一堆限制——不能自动迁移、不能校正、只能记条 warn 求用户自己改——全部消失。<br/>⚠️ **一个库，不是两个。** 我提过分出 `config.db` 护着账本，但那三条理由都弱：智能体拿到 `sqlite3` 之后同目录另一个库照样删得掉（是**减速带不是边界**）、库一年才 60MB 而它是查不是读整份、重置和同步的收益很小。残留风险用便宜办法顶：账本只加不写，AGENT.md 点名哪几张表别碰。<br/>⚠️ **落在运行时目录，不是安装目录。** 用户提过放安装位置，两条否决：① `bundle-macos.sh` 第一行就是 `rm -rf "$APP"`，**每跑一次开发脚本、每装一次新版，账本连同全部观测就没了**；② 实测往 .app 里写文件当场 `a sealed resource is missing or invalid`，而**辅助功能授权绑的就是签名**——签名一坏就读不到窗口标题，一半规则静默失效。而且换位置**换不来想要的东西**：智能体的写权限是「它被怎么启动的」的属性，不是路径的属性。<br/>⚠️ **`AGENT.md` 随程序发、每次启动刷进运行时目录**，跟库放在同一个文件夹：智能体拿到那个目录 = 同时拿到库和用法。每次覆盖，所以它**不会跟代码漂开**。<br/>⚠️ **默认值同时是模板**，这是它们存在的主要理由：AGENT.md 是手写散文，**散文一定会漂**（这个项目一天内被咬三次）；库里一行真实数据是代码建出来的，形状永远跟代码一致。<br/>⚠️ 那条示例计划 **`enabled = 0`**：样例不该有副作用，否则就是「用户没要求过却每小时打扰一次」。<br/>⚠️ **可执行文本只住在 `command` 表**，计划表和闹钟都只存名字——「这台机器上有哪些命令能被自动跑」永远只要看一个地方。<br/>⚠️ **带命令的计划条目错过了不补放**（`AlarmsList.Due` 里判的）：合盖两小时再打开，一条 22:00 的关机会当场执行。只提醒的条目照旧补放。<br/>⚠️ **改完必须把 `config.version` 加一**，程序每分钟看一眼才重装——否则「改了没反应」，而那正是这个项目最恨的那类失败 |
| I16 | **设置里一个红按钮「Configure online…」**：显示当前配置 → 写需求 → Copy all（含 AGENT.md）→ 把网页 AI 给的 SQL 贴回来 → 红色 Apply（2026-09-16 用户设计） | 程序里多了一条「执行任意 SQL」的路 | ⚠️ 它补上了配置进库之后唯一的缺口：**不是每个人手边都有能操作文件的智能体**。全程不用装任何东西。<br/>⚠️ **Copy all 把 AGENT.md 一起装进去**：网页 AI 还缺「规矩」（bump 版本、正则区分大小写、账本别碰、setting 是 JSON 片段）。9KB 而已，换来「只粘一次、不用上传文件」。<br/>⚠️ **账本护栏用事后核对，不用事前审查 SQL**：想解析 SQL 判断「它会不会动账本」是必输的（子查询、触发器、`DROP`、`ATTACH`）。执行前后各取一次账本指纹，对不上**整个事务回滚**——连同一起提交的正当配置改动一起作废，因为只挡一半会留下谁都没预期的半吊子状态。<br/>⚠️ **跑之前先 `VACUUM INTO` 备一份**；备份失败就**不跑**——没有退路的情况下执行外来 SQL 不值得。<br/>⚠️ **先把内存里的设置 flush 进库再跑**：设置在内存里有一份、退出时整份写回，不 flush 的话 SQL 改的那一行会被退出时覆盖掉。<br/>⚠️ **跑完不关程序**（用户原本提议关掉）：关窗口走 `OnClosing` → `Settle`，改一次配置就把**正在跑的那一轮作废**——代价和「改个提醒文字」完全不成比例。改成当场重装，只有 `layout` / `opacityPercent` 需要重启，在结果里多说一句。<br/>⚠️ **导出绝不含 `sample` / `title`**，这条由程序钉死、不靠用户记得删：那是用户开过的每一个窗口标题，而这段文字要被贴进网页对话框。程序名单另给，**写成注释不写成 INSERT**——程序名泄露「装了什么」，窗口标题泄露「在干什么」，不是一个量级。<br/>⚠️ 红只给 Apply：**通往危险的每一步都红，红就不再有意义**。<br/>⚠️ 要剥掉 markdown 围栏——网页 AI 几乎一定会包 ```sql，那三个反引号第一行就语法错误 |
| I17 | **退出路径靠一个显式标志幂等，不靠「里面每一步碰巧都能重跑」**（2026-09-16 实机撞到） | 多一个 bool | ⚠️ 事故的形状值得记死：`OnExit` 原来的幂等理由是「`Settle` 有 `_written` 挡着，**设置重写一遍也无害**」——那句话在设置还是一个 JSON 文件时成立。设置搬进数据库之后（I12），第二遍撞上**已经 Dispose 的连接**，`BeginTransaction` 当场抛；而那时 `Events` 已解绑，异常只落进那份本该是空的文本文件。<br/>⚠️ **一次改动作废了一条前提，而那条前提只写在注释里。** 这正是「日志该空着」那条设计救回来的一次——文本文件里有东西 = 出事了，一眼就看见。<br/>⚠️ 推论：**凡是「碰巧可重入」的地方都该换成显式标志**，因为「碰巧」会被下一次重构悄悄拿走 |
| I18 | **Apply 成功之后变灰，改了 SQL 才重新可按；备份一扇窗只做一次** | 想连跑两段 SQL 要改一下文本 | ⚠️ 用户 2026-09-16 连点了几下 Apply。那次侥幸没事**只是因为网页 AI 碰巧写的是 upsert**——换成普通 `INSERT` 就是几条重复行，而重复的目标 / 命令会安静地改变行为。**按钮跑完不变灰，就是在邀请这件事发生。**<br/>⚠️ **备份不能每次 Apply 都覆盖**：连按两下，第二份备份记的是第一次改完之后的状态，「回到什么都没改」就回不去了。一扇窗 = 一次改动会话，备份记的是会话开始前的样子 |
| I19 | **手动执行过的 SQL 记进 `applied_sql` 表**，连同用户当时写的那句需求；失败的也记 | 多一张表 | ⚠️ 它是**整个程序里唯一不可逆、且由外人写的动作**：观测丢了能重新采、轮次能从 sample 重建、账本只加不写，只有这一条是一次性的外来改动。**结果在库里，那句话却推不出来**——三周后「我到底跑了什么」除此之外没有第二个答案，而写它的还不是用户本人。<br/>⚠️ **不放文本日志**：那个文件的全部价值在于它是二值的（里面有东西 = 出事了），今天刚靠这个抓到退出路径那个 bug。塞进正常的成功事件，这个性质当场消失。<br/>⚠️ **不塞进 `event`**：`text` 是一列，多行 SQL 会把 `--query events` 冲烂；而且这件事天然有四个字段（原文 / 行数 / 成败 / 原因）。<br/>⚠️ **这张表必须进 `LedgerFingerprint`**：审计表要是被它审计的东西改得动，就等于没有——一句 `DELETE FROM applied_sql` 就把痕迹抹了。有测试守着。<br/>⚠️ **失败的记录要在回滚之后单独写**：写在事务里会跟着一起没掉，而「AI 给的 SQL 想动账本」恰恰是最值得留痕的那一种。<br/>⚠️ **意图和产物配在一起存**（用户写的需求 + 实际跑的 SQL）：光有 SQL 看不出 AI 有没有理解错那句话 |
| I20 | **智能体直接改库时，自己往 `applied_sql` 补一行**——它是唯一允许 `INSERT` 的账本表（2026-09-16 实操时发现的缺口） | 智能体多写一句 | ⚠️ 缺口的形状：`applied_sql` 只在 `SampleStore.ApplySql`（那扇窗）里自动记账，而 **AGENT.md 教智能体用 `sqlite3` 直接改库**——那条路根本不经过它。更糟的是 AGENT.md 还写着「账本表一律别碰」，于是**一个听话的智能体会一点痕迹都不留**。我自己第一次实操就撞上了。<br/>⚠️ 结论不是「把它从账本里拿出来」：`UPDATE` / `DELETE` 仍然必须挡住，**能抹掉自己的记录就不叫记录**。改成「只许 `INSERT`」——追加是留痕，改写是毁证。<br/>⚠️ 这件事值得提到 AGENT.md 最前面跟 bump 版本并列：两条都是「不做就白做」——一条是用户看不见效果，一条是三周后没人说得清为什么 |
| I21 | **手动执行的记录从 `applied_sql` 表挪进 `applied-sql.log` 文件**（2026-09-16 用户：把 SQL 记在 SQL 表里让人不舒服） | 刚清干净的目录里多一个文件 | ⚠️ **挪出来反而更硬，不只是观感**：SQLite 够不着普通文件（`ATTACH` 只能挂另一个数据库），所以这份记录**根本不在任何一句外来 SQL 的射程之内**。原来靠账本指纹拦 `DELETE FROM applied_sql`——那是「拦得住已知的那一招」；现在是**压根没有那一招**。<br/>⚠️ 顺带：库坏了要修的时候，「我到底干过什么」不在那个坏掉的库里面。<br/>⚠️ **跟 `itamiben.log` 不是一回事，别合并**：那份的全部价值在于它是二值的（有东西 = 出事了），今天刚靠它抓到退出路径的 bug；这份是正常且必要的流水账，天天都该长。合在一起两个性质一起没。<br/>⚠️ **不滚存、不截断**：别的日志滚存是怕被高频事件灌爆，这份一年几 KB；而滚掉审计记录，滚掉的正好是最早、最难回忆的那些。<br/>⚠️ 记账放在 **App 层**：Core 里一个 `File.` 都没有，这条纪律换来的是「单元测试碰不到用户的真实文件」 |
| I22 | **`BuildGoals` 必须可以重复调**（清空再铺，并把选中项带过去） | — | ⚠️ 2026-09-16 实机撞到：它原来只在启动时调一次，所以**只往里加、从不清空**；接上「改完配置当场重装」之后，界面上的目标列表**翻了一倍**。<br/>⚠️ 跟 I17（退出路径不幂等）**是同一个形状**：**一个函数原本「只跑一次」，后来被第二个调用方接上，而它从来没为第二次做过准备。** 这个项目一天之内撞了两次，值得当成一类来防：**接新调用方之前，先问这个函数跑第二遍会怎样。**<br/>⚠️ 重铺要带选中项：不带的话改一次配置就把用户刚选的目标弄丢了 |
| I23 | **拆掉「动了账本就回滚」那道闸**；记账并进 `itamiben.log`（2026-09-16 用户拍板，推翻 I16 / I19 / I21 的一部分） | 一句写歪的 `UPDATE total` 真的会落盘 | ⚠️ **拆它的理由是它只守住两扇门里的一扇**：智能体走 `sqlite3` 直连那道门根本不经过它，而那正是 AGENT.md 教的路。更糟的是它让文档里「动账本会被挡回去」变成**一句只在某一条路上为真的话**——**一道假装是承诺的闸，比没有闸更坏**。<br/>⚠️ 剩下的保证只有一条，但那一条是真的：**整段一个事务，要么全落要么一条都不落**。退路换成两样都在库外面的东西：**跑之前那份备份** + **`itamiben.log` 里那条记录**。出了事看得见、回得去。<br/>⚠️ AGENT.md 相应改口：从「会被拦下」改成「**没有东西拦你**，但你干的每一件事都被记着」。**把假保证换成真记录**。<br/>⚠️ 记账并进 `itamiben.log`（不再单开 `applied-sql.log`）：智能体只该有**一个**地方要写。代价是这个文件不再是二值的（「有东西 = 出事了」那个性质没了，而它今天刚抓到一个 bug）——**知情接受**，找毛病改成翻 `error` / `FAILED` 那几行或者 `--query events` |
| I24 | **`--query` 只给二进制的库**，纯文本文件不配有出口；**面向用户的文档里不教任何命令**（2026-09-16 用户定） | 想看日志就得自己打开文件 | ⚠️ `--query log` 是**把 `cat` 包装了一遍，还得教人怎么用**。日志是纯文本，用户直接把文件拖给 AI 就行。<br/>⚠️ 剩下几条留着是因为**库是二进制的**，而且 `sqlite3` 不是哪儿都有（Windows 就没有）；`--query minutes` 更是**非它不可**——它要重放判定引擎，任何 SQL 都算不出来。<br/>⚠️ 两份 README 里「调试出口」整节删掉，换成一句**「觉得不对劲就把 `itamiben.log` 丢给 AI」**：这个程序的配置本来就是让智能体改的，查问题没理由反过来要求用户学命令。命令行那几条移进 AGENT.md——**它们是给智能体的，不是给用户的** |
| I25 | **窗口标题是文案，不许被任何逻辑当标识符用**；标题改成日文的「痛みを知らせる」，单实例提到前台改按**进程名**找（`Process.GetProcessesByName` + `MainWindowHandle`），`FindWindow` 那两个 P/Invoke 删掉 | 找到的是「这个二进制的另一个实例」，而不是「标题正好是这串的那扇窗」——理论上多开一扇没标题的辅助窗口会先被选中，本程序没有这种窗口 | ⚠️ 2026-09-16 用户问「日文标题会不会找不到」。**字符集从来不是问题**：`CharSet.Unicode` 绑的是 `FindWindowW`，全程 UTF-16，日文和原来的中文、破折号是同一类；真正脆的是 `FindWindow` 做的**整串精确匹配**——标题动一个字，Windows 上第二次双击图标就**静默地什么都不发生**，不报错不写日志。<br/>⚠️ 这是本项目第三次踩同一个形状：**一件事写了两份靠人记得同步**（I17 的设置、I22 的目标列表）。这次更隐蔽，两份里有一份是「给人看的文案」，它天生就要改。<br/>⚠️ 进程名 = `AssemblyName`，那个是**钉死的**（macOS 的 `localizedName` 报的就是它，库里存的、规则里匹配的都是它），所以这条依赖挂在一个永远不动的东西上。<br/>⚠️ **别再把标题接回任何逻辑**。<br/>⚠️ 前缀 `ItamiBen — ` 一并去掉：程序名在 Dock / 任务栏里本来就有，而主窗口是 `WindowDecorations="None"`，**macOS 上这行字根本看不见**。<br/>⚠️ 跟别的 Windows 代码一样，**没在真机上跑过** |
| I26 | **退出记录写在 `OnExit` 里，不写在任何一条退出路上**（2026-09-16 实测发现） | 信号那条路上，stop 事件从「信号一到就写」挪到了「settle 真的开始时才写」——UI 线程若彻底卡死，记录会落在 `Events.Error("stop", "Failed to settle on signal")` 上而不是 info | ⚠️ 原来 `Events.Info("stop", …)` **只在 POSIX 信号处理器里**，于是 `pkill` / 关机留得下记录，**点 × / Cmd+Q 什么都不留**。而 `OnExit` 的注释开头就写着「三条路都会调它」——记账却挂在其中一条上。<br/>⚠️ **症状是沉默的**：`event` 表里历史上每一条 stop 都是 `SIGTERM received`，一条手动关闭都没有，而手动关闭恰恰是最常用的关法。查「上次程序是怎么没的」时，**缺记录和「被 kill -9 了」长得一模一样**。<br/>⚠️ 是**对照 ActivityWatch 的窗口时间线**才定位的：AW 显示 18:43:58 焦点离开 ItamiBen 之后它再没出现过（= 手动关掉了），而库里那一刻是空白。用户当时说「这条记录应该有的，可以对照 aw，不要猜测」——**外部独立观测能证伪自己程序的记录**，这条值钱。<br/>⚠️ 同一个形状的第四次（I17 / I22 / I25）：**一件事挂在其中一个入口上，而不是共用的那一处**。<br/>⚠️ `window closed` 那条路**只做了代码验证，没在真机点过 ×**——信号那条路实测过了 |
| I27 | **报给用户的行数只算用户那段 SQL，不含程序自己的版本号记账**；0 行要当成显眼的结果说出来，不是「成功」的一种 | 多一个局部变量，`TotalChanges()` 多读一次 | ⚠️ 2026-09-16 实测：`ApplySql` 在 bump `config.version` **之后**才算差值，于是返回值恒等于「真实行数 + 1」——**这个数字永远不可能是 0**。而「我这句到底匹配上没有」正是它唯一该回答的问题。<br/>⚠️ 触发它的是 `UPDATE goal SET enabled = 0 WHERE name = ' 番茄钟 '`：网页 AI 给中文名加了前后空格，一行没匹配，界面报「Applied. 1 row(s) changed, configuration reloaded」。**一次静默失败被报成了成功**，用户只能靠「我看窗口没变」才发现。<br/>⚠️ 形状：**把自己的记账混进给用户的读数里**。跟 I11「凡是能推出来的就不另记一份」同源——那条管的是别存两份，这条管的是别把两份加在一起报出去。<br/>⚠️ 0 行**不一定是错**（SELECT、DDL、本来就已经删掉的行都是 0），所以措辞是「跑通了但什么都没改，通常是值对不上」，给方向而不是判死刑 |
| I28 | **改完配置必须去 `event` 表确认程序真的看见了**——一分钟内应出现 `reloaded at version N`；对不上就**停手别再写库**，改走 Configure online 让用户 Apply （2026-09-18 Windows 实机那晚栽出来的） | 每次改配置多一次查询、多等最多一分钟 | ⚠️ **事故经过**：智能体的 shell 跑在一个沙箱里，`%LOCALAPPDATA%` 被**静默重定向**到别处，于是几个小时的配置全写进了一个影子库，而用户那台真正的 ItamiBen 一直在给自己播种空默认值。<br/>⚠️ **最毒的地方是期间每一个本地信号都显示成功**：UPDATE 报「改了 1 行」、`SELECT` 读得回来、`--query config` 打得出来——因为它们读写的是**同一个影子库**，自洽得毫无破绽。**一个只照自己镜子的系统永远说自己没病。**<br/>⚠️ 那条事件**是程序写的，不是智能体写的**，所以它不会跟着一起骗人——这是唯一一条能跨出「智能体自己那份文件系统视图」的检查。判据不是「我写成功了」，而是「**那个我控制不了的进程，承认它看见了**」。<br/>⚠️ 这条跟 I25 / I26 / I27 是同一族的下一层：前三条是「一件事写了两份，靠人记得同步」，这一条是**连「两份」都看不见**——你以为的那一份根本不是用户的那一份。<br/>⚠️ **别把它做成程序里的一道校验**：程序无法知道智能体看到的是哪个文件。它只能是**智能体的纪律**，所以写在 AGENT.md 的规则 1 里，不写进代码 |
| G11 | **松手之后把窗口整个夹回屏幕工作区**（v3 的 `ClampIntoScreen`，2026-09-16 补搬） | 拖出去的部分会被弹回来，不能把窗口停在屏幕外 | ⚠️ **必须等拖动停下来再夹（250ms 没动过 = 松手），绝不能一边拖一边夹**：一边拖一边夹的话窗口永远被摁在当前这块屏的边界内，就永远到不了「一半以上落在另一块屏上」那个状态，而 `ScreenFromWindow` 正是按这个判断该归哪块屏——结果是**窗口再也拖不到第二块显示器上去**。v3 的用户是双屏，这不是理论风险。<br/>⚠️ 夹回的目标是 `ScreenFromWindow` 那块屏的**工作区**，**不是 `Screens.All` 拼起来的外接矩形**：多屏排布可能不是完整矩形，外接矩形会把两块屏之间的空洞也算成合法位置。<br/>⚠️ **单位不能混**：`Position` / `WorkingArea` 是**物理像素**，`FrameSize` / `ClientSize` 是**逻辑单位**，差一个 `Screen.Scaling`。不换算的话缩放不是 100% 的屏幕上会算错窗口多大。<br/>⚠️ 夹回时自己挪窗口会再触发一次 `PositionChanged`，要用 `_clamping` 挡掉，否则每夹一次又排一次 settle，来回震荡。<br/>⚠️ 这件事**只能有一份实现**：恢复上次位置那条路也走同一个函数（`Opened` 里调），别在那儿另写一套「留 80 像素在屏幕上」的夹法 |

## E. 闹钟与声音（2026-09-16 从 v3 搬过来）

规格和大部分护栏在 `../ItamiTimer/DECISIONS.md` 的 E 组，**那些条目原样有效，别重写**。
这里只记 v4 特有的决定，以及**哪些故意没搬**。

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| E1 | **`AlarmClock` 放 Core，不放 App**（v3 放在 App 里） | — | ⚠️ 别挪回 App。它零 UI、`now` 是参数、纯状态——完全符合 Core 的条件，放 App 只会让它测不到，而 v4 的 App 测试工程本来只该测「App 层里的纯逻辑」那一点点 |
| E2 | **闹钟判断挂在采样节拍上（100ms），不是整分钟节拍** | 每拍多一次整数比较，可忽略 | ⚠️ 别照搬 v3 的 L13 顺序。那条顺序是为了排 AW 查询 / 清单 / 三声通知 / 关机命令的先后，**v4 这四样一样都没有**；照搬过来只会把误差从 100ms 放大到 59 秒，还带一套没有对应物的排序规则 |
| E3 | **新增 `IsArmed(now)`，界面上把「上弦了」和「黄针残影」分开写**（`⏰ 03:44` vs `⏰ 03:44 · off`） | 多一个查询 | ⚠️ 别省掉那个 `off`。v3 的 E7 明说过期闹钟只剩「黄针残影」，而残影和真上弦在盘面上**长得一模一样**——不写出来用户读不出它还作不作数。v3 靠 ToolTip 只显示时刻，那是它的 E15 要解决的老问题，别继承 |
| E4 | ~~**不搬「三声通知」**~~ **2026-09-16 推翻**：三声都搬了（Focus complete / Break over / Idle Warning），各响 2 遍 | — | ⚠️ 当初不搬的理由是把 D1 读成了「整个程序不许有任何提示」——**那是误读**（见 D1 的澄清）。用户要的提示一直跟 v3 一样。<br/>⚠️ 三声各 **2 遍**，比闹钟的 4 遍少：它们**各自落在一个留在屏幕上的状态上**（环满了、淡蓝块、格子不再长），漏听还能看回来；闹钟响完什么都不留。<br/>⚠️ Idle Warning 只在**空闲 60~180 秒**那一段响：过了 180 就是真离开了，那一段既不计入也不算跑偏，**催也没用** |
| E5 | **没搬 `Activate()` 和 `MacAudio.Forget()`** | — | ⚠️ 不是漏了：前者是给 v3 的「打开 Execute 开关」用的，后者是给「音量变了要重建 SoundID」用的，**v4 两个功能都没有**。留着没人调的代码等于留一个会慢慢失真的说明——v3 自己删 `Classify` 时记过同一条（「a guardrail watching over code that never runs」） |
| E6 | **`Sound.Duration` 拆出一个吃 `Stream` 的重载**，纯计算那一半因此可测；为它新开了 `tests/ItamiBen.App.Tests` | 多一个测试工程 | ⚠️ 那个工程**只测 App 层里的纯逻辑**（音频文件头这类），判定和计时的测试一律留在 Core.Tests。⚠️ 也别把文件头解析挪进 Core——它跟「判定与计时」没关系，挪进去 Core 的边界就开始漏 |
| E7 | **滴答是两个开关**（2026-09-16 用户澄清，跟 v3 一致）：桌面上的**喇叭图标挂「跑偏才响」**，菜单 / 设置里的 **Force ticking 无条件响**。合起来是 `ForceTicking \|\| (TickEnabled && 正在跑偏)` | — | ⚠️ 我一度只搬了「一直响」那一种，理由是把 D1 读成了「不许有任何提示」——**那是误读**，见 D1 和 D6。<br/>⚠️ 两个开关别合成一个：一个是环境音（钟在走），一个是提示音（你跑偏了），语义不同。<br/>⚠️ `Tick.Stop()` **只能挂在开关里**（v3 的 E13）：Windows 上它是 `PlaySound(null)`，停的是本进程在 winmm 单通道上正在放的**任何东西**——包括正在响的闹钟。放进 `ApplyChrome` 就等于「点一下图钉把正在响的闹钟掐了」。<br/>⚠️ 音量一变必须 `MacAudio.Forget`：SystemSoundID 在创建那一刻就把音频吃进去了，覆盖同名文件没有效果，症状是**拖音量滑块什么都不发生** |
| E8 | **闹钟到点跑命令的开关不持久化，每次启动一律是关的**（v3 的 E8），而且做成**普通字段不是 Settings 属性** | 想到点关机就必须在本次会话里手动打开一次 | ⚠️ **持久化它是事故**：那条命令多半是关机/重启，重启之后被上一个会话的设置拍死，而你根本不知道它还开着。<br/>⚠️ v3 是放在 Settings 里靠 `Load` 强制复位——那要**靠人记得**。做成普通字段之后，「不持久化」这件事**结构上做不到反面**。<br/>⚠️ 命令**只执行第 0 条**（v3 的 E9）：那是个收藏夹不是配置格式，换命令靠重排文件顺序，别加界面去选 |
| E9 | **到点跑命令和响铃是二选一**，不叠加 | 开着命令那一轮就没有铃声 | 响铃是「提醒你自己动手」，跑命令是「替你动手」——两件事同时发生没有意义。跟 v3 一致 |
| E10 | **右键菜单那一项不显示命令，但设置窗口的 Command 卡显示**（2026-09-16 定稿） | — | 用户先说「不需要显示具体命令」，那是针对**菜单项**说的；后来要求设置界面照抄 v3，而 v3 的 E14 正是把命令显示在那儿，理由是「**这个值多半是关机命令，按下开关之前有权知道按的是什么**」。两处各按各的来：菜单里是纯开关，设置里连原文一起给。<br/>⚠️ 那不是说明文字，是**值**本身（D6 管的是前者）。没配好就直说没配好 |
| E11 | **设置窗口照抄 v3**：一张卡一个设置，**一个字说明都没有**（标题 + 控件，剩下的留给猜）；顺序 Force ticking 打头、Command 收尾 | 开关在菜单和设置里各有一份 | ⚠️ **两处必须走同一个 `Set*` 入口**——右键菜单、右上角图标、设置窗口读写的是同一个状态，绕过入口就会分家（G8 那个病）。<br/>⚠️ **`Topmost="True"` 不能省**：主窗口可能被钉住，普通模态窗会**整个沉到它下面**——屏幕上什么都不出现，点齿轮看着像没反应（v3 2026-07-28 实际撞到过，用户 2026-09-16 又提了一次）。<br/>⚠️ **`CanMinimize="False"` 不能省**：这扇窗是 `ShowDialog` 出来的，最小化不只是藏起来——模态开着时主窗口全程禁用，而它又 `ShowInTaskbar="False"`，最小化之后连个能点回来的条目都没有。净效果是主窗口每个按钮都死了而且看不出原因。`CanResize="False"` **不蕴含**这一条 |
| E12 | **改动当场生效当场落盘，没有「确定 / 取消」**；挑音色**当场放一遍** | 手滑改错要自己改回来 | 这是三个外观类的值，为它维护一份「还没提交的草稿」不值当。<br/>⚠️ 试听是 `Play` 一遍不是 `Repeat` 四遍——**这是在挑音色，不是在排练打扰**。<br/>⚠️ 音量改了必须当场试听：合成是按音量烘焙的，不听一下不知道调到哪儿了 |

## D. 钟面视觉（2026-09-15 定稿，规格见 DESIGN §5）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| D1 | **休息画成淡蓝色块，紧跟在预计达成点之后，全程可见**（不是只在休息时才出现），不画扇形 | 环上多一种颜色 | ⚠️ **这条是承重的，看起来只是换了个颜色，实际同时承载三个信息**：<br>① 分针 → 淡蓝起点 = 还要专注多久<br>② 淡蓝长度 = 休息多久<br>③ **淡蓝尾 → 环终点 = 还剩多少余量**<br>改成「只在休息阶段才画这块蓝」，②③ **当场塌掉**，而且**不报错**——盘面照常好看，只是从此再也读不出余量。「截止线」也会重新变成需要单独画一条线的东西（现在环的终点就是它）。<br>⚠️ 别给**这块蓝**加闪烁/警告：它往环尾退这件事本身就是惩罚。<br/>⚠️ **2026-09-16 澄清**：这句管的是**淡蓝块**，不是「整个程序不许有任何提示」——我一度把它读成后者，还据此拒了跑偏闪烁和跑偏滴答。用户指出那是误读，两者都是他一直要的（见 D6）。|
| D2 | **休息块的「预告」和「已发生」必须两种画法**（预告淡一档或描边，已发生填实） | 多一种画法 | ⚠️ 不分开的话，分针走进休息区时**画面毫无变化**，用户无法确认休息到底开始没有。v3 对承诺弧有同样的区分（`Gray` = 未发生的未来） |
| D3 | **灰色承诺弧保留**，填满「分针 → 淡蓝起点」 | — | ⚠️ **别省**。省了那段就是空白，跟「余量」那段的空白**长得一模一样**——两个完全不同的含义撞色，三段读数塌成两段 |
| D4 | **淡蓝在暗色主题下单独调一档** | 多一组主题资源 | ⚠️ 别两个模式共用一个淡蓝。饱和/浅色在深底上容易发灰发脏，v3 的 M 组已经踩过（提示条底层最后是两套色） |
| D5 | ~~**一格里绿 / 红 / 留白按秒数堆叠**~~ **2026-09-16 用户推翻，回到 v3 的画法**：一格**一块桶板**，颜色走绿→黄→红三段色阶、高度按 `>40 / >20 / >0` 分三档；人不在画**空心虚线框**；没记录什么都不画 | 一格只表达一个读数（档位），不再同时显示三段秒数 | ⚠️ 用户原话：「表示每分钟专注比例的颜色、离开时的空白框都不见了，而且**没有必要每秒添加绿色的高度**」。<br/>⚠️ **颜色和高度编码同一个量**：一个给正常视觉，一个给所有人——红绿是最常见的色盲混淆对（约 8% 的男性），亮度差是颜色之外的第二个信号。<br/>⚠️ 桶板最矮 1/2，**绝不能取 0**：「没记录」画的就是「什么都不画」，零高度会跟它撞上，而这正是最不能混的一对。<br/>⚠️ 档位判定写在 `MinuteCell.Tier` 里、**只写一份**：v3 当年因为 CLI 和钟面各写一遍，同样的阈值、argmax、平局规则抄了两份 |
| D6 | **跑偏就让钟面一秒一翻**（半反色：只翻盘面 / 刻度 / 指针），外加**跑偏时滴答**（2026-09-16 用户澄清，照搬 v3 的 §8.9） | 跑偏期间画面一直在动 | ⚠️ **是「翻」不是「设成反色」**：稳定反着的钟面看两分钟就变成壁纸了，抓不住眼睛；**闪**才抓得住（v3 的用户 2026-08-29 撤掉「5 秒一次」那版的全部理由）。一秒一翻 = 周期 2 秒 = 0.5 Hz，远在光敏性癫痫的 3 Hz 风险区间之外。<br/>⚠️ **判据必须是 `== OffTask`，不能写成「不是 Focused」**（v3 的 N4）：那样「人不在」「读不到」也会算跑偏——锁个屏钟面就永远闪下去，而账本那段一秒都没扣，**屏幕跟账本对着说反话**。<br/>⚠️ **色环、木框、淡蓝块、黄针、小红圈一概不翻**：那是账本本身，翻了就把「绿=专注、红=跑偏」这套语义拆了。骨牌也不翻，它压根不在钟面上。<br/>⚠️ **休息阶段必须显式清掉跑偏标志**：它只在「专注阶段记下一秒」那条路上更新，休息那条路不碰它——留着上一个值就会**整个休息期间一直闪**（2026-09-16 写的时候就踩了这个） |
