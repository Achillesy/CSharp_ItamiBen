# DESIGN — ItamiBen

⚠️ 本文件不进 git（用户 2026-09-15 定）。

## 1. 定位

带强制约束的番茄钟。**选一个**允许的小目标 → 提交任务 → 程序**自己读前台窗口和标题**，
只有命中规则的时间计入。偷懒不弹窗不出声，只有钟面上的红格和越滑越远的截止弧。

副标题 **"Too dumb to make excuses for you"**：规则是你自己事先写的，运行中不讲情面。

### 1.2 为什么不再用 ActivityWatch

前作 ItamiTimer（v3，`../ItamiTimer`，已定版）把 AW 当地面真相。用户 2026-09-15 报了一个
缺陷：全屏玩 mame 的 6 分钟被判成「学习经济学」，任务还靠它「达成」了。

⚠️ **当时的归因是错的，2026-09-15 下午实测推翻，这里记着以免有人照着错的理由做设计。**

**曾经的说法**（v3 的 DESIGN §16.6 里还留着）：AX 是同步 IPC 进对方进程且没设超时，
全屏 SDL/OpenGL 应用不理会 accessibility 请求，把 AW 的轮询循环卡死了。
当时明确标注过「符号表证据 + 机制推理，**没有拿 mame 实证复现过**」。

**实测结果**（13:40:03~13:53:12，13 分钟真实全屏 mame，两边同时观测）：

| | 看到的 |
|---|---|
| 本项目探针 | `app=mame`，title=`Street Fighter II': Champion Edition (World 920513) [sf2ce] - MAME 0.289 (LP64)`，**785 次采样、784 次 `ok`** |
| ActivityWatch | 一条 **791.6 秒**的 `Terminal` / `mame0289-arm64 — -zsh — 120×32` |

**AX 根本没卡。** mame 对 accessibility 请求响应良好，标题一读就有。

**真正的原因**：`aw-watcher-window-macos` 的符号表里是
`_NSWorkspaceDidActivateApplicationNotification` + `AXObserver` / `AXFocusedWindowChanged`
/ `AXTitleChanged`——**它两半都是「订阅通知」，不是「主动轮询」**。mame 是从 Terminal
里启动的裸可执行文件、不是正常的 .app bundle，激活时不发 app 切换通知，于是 AW 永远
停在 Terminal 上，整整 13 分钟都以为用户在用终端。

⇒ **结论变了，但结论的方向没变**：AW 不适合当这个 app 的地面真相。只不过理由从
「它会被卡死」换成了**「它是事件驱动的，看不见不发通知的 app」**。

⇒ **而这直接决定了 v4 的一条硬规矩**：**必须主动轮询，绝不订阅通知**（见 §3、DECISIONS B4）。
轮询看得见订阅看不见的东西——这是实测出来的，不是设计偏好。

⚠️ 12:08 那次事故里「AW 的窗口桶游标冻了 402 秒」这个现象**仍未解释**（今天这次
AW 的事件是连续生长的）。v3 已定版，不再追查；v4 两种形态都免疫，不影响设计。

代价要明说：**交出了「客观审计」那一半**。程序没运行的时间不再有任何记录，也无从事后
核对——v3 的 Principle 3（关掉界面不影响结果）在这里不成立。换来的是任务进行中的观察
反而更准（无 6~12 秒滞后、无 watcher 猝死、无 afk 桶脏数据），以及 v3 那一大串为了
迁就 AW 而存在的护栏全部归零。

用户 2026-09-15 明确接受这笔交易，并同时决定**不再做历史统计**。

## 2. 已经量到的事实（2026-09-15，M1 Mac mini / macOS 26.5.2）

**这些是探针实测的，不是推断，直接可以拿来设计：**

| 事实 | 值 |
|---|---|
| `NSWorkspace.frontmostApplication` | **零权限、非阻塞**；`localizedName` 跟 AW 记的 `app` **逐字相同**（实测 `Claude` / `Google Chrome`） |
| `CGWindowList` 的 `kCGWindowOwnerName` | 零权限拿得到 |
| `CGWindowList` 的 `kCGWindowName`（标题） | 无 Screen Recording 授权时**全是 nil** |
| `AXUIElementSetMessagingTimeout(el, 0.25)` | **返回 0（成功）**——这个工具存在、能用 |
| `aw-watcher-window-macos` 的符号表 | 有 `AXUIElementCopyAttributeValue` / `AXFocusedWindow` / `AXTitle`；**没有 `CGWindowListCopyWindowInfo`，也没有 `AXUIElementSetMessagingTimeout`** ← 它被卡死的原因 |
| 无授权时 `AXUIElementCopyAttributeValue` | 返回 `-25211`（`kAXErrorAPIDisabled`），**23.5ms 干脆返回，不挂起** |
| `AXIsProcessTrusted()` | 可以直接查授权状态，用来决定要不要提示用户 |

⚠️ **已验**（2026-09-15，见 §1.2）：AX 对全屏 mame **不卡**，784/785 次 `ok`。
`SetMessagingTimeout` 仍然要设——那是防御，不是主线——但 worker 不需要为「彻底挂住」
做特殊设计。

### 2.1 第一个探针的实测结果（2026-09-15 13:18）

| 问题 | 答案 |
|---|---|
| 本机读得到别的 app 的窗口标题吗 | **读得到**，前提是辅助功能授权 |
| 标题跟 AW 记的一样吗 | **逐字节相同**（67 字节 vs 67 字节，`==` 为 True）：<br>`曼昆经济学原理-习题解析.pdf - Right view - Google Chrome`<br>⇒ **§5 第 2 条通过，v3 的规则可以原样搬过来** |
| app 名跟 AW 记的一样吗 | 相同（`Google Chrome` / `Claude` / `System Settings`） |
| 拨开关之后要重启进程吗 | **不用**。实测 13:18:15 `trusted=False` → 13:18:16 `trusted=True`，下一拍就生效 |
| 没授权时能读到什么 | 只有 app 名；**自己的标题读得到**（读自己进程的 AX 不需要授权），别人的一律 `APIDisabled` |

⚠️ **`kAXErrorAPIDisabled` 的含义是「本进程未被信任」，不是「那个 app 不给」。**
症状是「能读自己、读不到别人」——第一次见很容易误判成目标 app 的问题，别再绕这个弯。

### 2.1.1 采样必须锚在墙上时钟，不能用固定间隔定时器

同一批日志里量到的：13 分钟里 `DispatcherTimer(1s)` **漏掉了 5 拍**，分别在
13:40:52 / 13:43:34 / 13:46:20 / 13:49:08 / 13:51:53——**间隔极其规律（约 165 秒一次）**，
典型的累积漂移：每拍比 1 秒多一点点，攒够 1 秒就整秒跳过去一次，而且**不报错**。

⇒ 产品里采样要像 v3 那样**用一个高频 sampler + 「这一秒采过没有」的哨兵**锚在墙上
时钟上，不能天真地 `Interval = 1s`。探针阶段可以先这样，但别带进产品。

### 2.2 ⚠️ ad-hoc 签名让授权绑在 cdhash 上（开发期最大的坑）

`security find-identity -v -p codesigning` → **0 valid identities**，只能 ad-hoc 签名。
于是**每次改代码重编，cdhash 就变，辅助功能授权当场作废**，而系统设置里那条记录
**看着还在**、实际认的是旧指纹——极其具有迷惑性，2026-09-15 为此绕了好几轮。

应对（已落地）：
- `./run-macos.sh --run-only` 只启动不重编，保住授权
- 启动时若未授权就自动注册一次，保证列表里那条绑的是当前二进制
- 换指纹之后要 `tccutil reset Accessibility com.achillesy.itamiben` 清掉旧记录再重来

**已解决（2026-09-15）：自签名代码签名证书 `ItamiBen Development`**，存在登录钥匙串。

实测对照，这是整件事的关键证据：

```
ad-hoc 签名：  designated => cdhash H"1989850c..."
              ↑ 只有指纹，连 bundle id 都没有 ⇒ 每次重编必失效

证书签名：    designated => identifier "com.achillesy.itamiben"
                            and certificate root = H"c301bf19..."
              ↑ 绑 bundle id + 证书 ⇒ 跟二进制无关
```

改一行源码重编之后实测：**cdhash `a7803050…` → `efa7448d…` 变了，DR 逐字不变。**

⚠️ **这张证书只是本地开发工具**，跟对外发布无关：别人的机器不信任它，Gatekeeper
照样拦。也不能证明任何「原创性」——谁都能生成一张同名的。

⚠️ **对外发布时这条会变成真问题**：TCC 授权绑 DR，所以**没有稳定证书的话，用户每装一个
新版本都要重新授权一次辅助功能**，而且没有任何提示，只表现为「标题读不到、什么都不记」
——正是 2026-09-15 我们自己踩的那个坑，只不过踩的人变成用户。所以对这个 app，
买 Apple Developer ID 的首要理由是**更新后功能不会静默失效**，不是「没有吓人的警告」。
（Windows 无此问题：读前台窗口标题不需要任何授权。）

#### 证书没了怎么重新生成

私钥只在登录钥匙串里，没有别的备份（丢了的代价仅仅是重新授权一次）。重建：

```bash
cat > cert.conf <<'EOF'
[req]
distinguished_name = dn
x509_extensions    = v3
prompt             = no
[dn]
CN = ItamiBen Development
O  = Achilles
[v3]
basicConstraints   = critical,CA:false
keyUsage           = critical,digitalSignature
extendedKeyUsage   = critical,codeSigning
EOF
openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 7300 -nodes -config cert.conf
# ⚠️ 必须加 -legacy：OpenSSL 3.x 默认的 AES-256+SHA-256 打包 macOS 读不了，
#    报 "MAC verification failed during PKCS12 import (wrong password?)"
openssl pkcs12 -export -legacy -inkey key.pem -in cert.pem -out itamiben.p12   -name "ItamiBen Development" -passout pass:itamiben
security import itamiben.p12 -k ~/Library/Keychains/login.keychain-db   -T /usr/bin/codesign -P "itamiben"
security add-trusted-cert -r trustRoot -p codeSign   -k ~/Library/Keychains/login.keychain-db cert.pem
rm -f key.pem itamiben.p12        # 私钥别留在磁盘上
tccutil reset Accessibility com.achillesy.itamiben   # DR 变了，要重新授权一次
```

删除：`security delete-identity -c "ItamiBen Development"`，删掉就回到 ad-hoc 状态。

## 3. 核心设计：把 AW 焊死的那两半拆开

| | 来源 | 权限 | 阻塞 | 失败时 |
|---|---|---|---|---|
| **app 身份** | macOS `NSWorkspace.frontmostApplication`／Windows `GetForegroundWindow` + exe 名 | 不要 | 不会 | 基本不会失败 |
| **窗口标题** | macOS AX（`SetMessagingTimeout` 250ms）／Windows `SendMessageTimeout(WM_GETTEXT)` | macOS 要 | 有超时 | 标题置空 |

两条**独立**采样。主钟每秒只读第一条（微秒级）；标题由一个**单独的 worker** 尽力而为。

**关键规则：app 变了而标题还没跟上，就当标题未知。**

这样 v3 §16.6 那个 bug **由构造决定不可能发生**——全屏应用把 AX 卡住时，NSWorkspace
照样报出你在 mame，规则匹配不上，直接判跑偏。

### 3.1 顺带没了的一整层复杂度

本地每秒一个采样 ⇒ 不需要「区间事件摊平成秒」，不需要预测、不需要外推、不需要
245 秒环。v3 的 `AwMirror` / `MirrorFeed` / `Backfill` / `During` 整层不存在。

## 4. 判定与计时模型（2026-09-15 定稿）

### 4.1 三层数据，粒度各不相同

| 层 | 粒度 | 内容 | 可变性 |
|---|---|---|---|
| **采样** | 每秒 | `(app, title)` 匹配选中目标的规则 → `Focused` / `OffTask` | **当场定死，永不改写** |
| **格子** | 每分钟 | `focusedSeconds` + `sampledSeconds`（各 0~60） | 那一分钟走完即封盘 |
| **累计** | 整数秒 | 本轮有效秒数 | 实时累加，每分钟刷新显示 |

⚠️ **粒度就用整数秒，不要更细**。理由不是「方便」：**采样本身就是 1 Hz，再细的单位是
假精度**，只会让人误以为测得更准。

⚠️ **必须存两个计数，不能只存一个判定**——要分开的是**三种**状态，不是两种：

```
focused=40, sampled=60  →  专注 40 秒、跑偏 20 秒
focused=40, sampled=40  →  专注 40 秒、另外 20 秒没采到（睡了 / 程序没跑 / 漏拍）
```

**没采到的秒既不计入也不算跑偏。** 这一条同时挡住两件事：「睡一觉刷满番茄钟」
（v3 的 H4a 专门为此写过一条），以及「程序自己漏拍却算用户跑偏」。

### 4.2 每一秒当场定死——v4 跟 v3 最大的结构差异

v3 的 245 秒镜像和「每分钟重读四分钟」，**唯一存在理由**是 AW 的数据会事后改写
（窗口事件滞后 6~12 秒、afk 回填 3 分钟），所以每一秒的判定在几分钟内都可能被推翻，
必须留一个可重画的窗口。

v4 是进程内同步读取，**没有任何东西会事后改写**。于是整层消失：`AwMirror` /
`MirrorFeed` / 预测三规则 / 环形缓冲 / `carryForward` / `QueryWindowSeconds` /
每分钟重读，全部不需要。

顺带白捡：v3 花了两天 debug 的「闪烁跟不上」（外推值被锁死在前沿，DECISIONS O18）
**在 v4 里结构上不可能发生**，因为没有外推。

### 4.3 两小时环与达成截止线

**120 格 = 120 分钟 = 钟面两圈 × 60**，索引就是「从 Start 起第 N 分钟」。
**不环绕、不归档、不塌缩。**

v3 的归档机制整个消失——H7 的 `[180, 3780)` 结算范围、H8 的盘面塌缩、以及
§15.5 那次「归档瞬间账会跳」的事故，全部不存在。换来的性质：

> **转完两圈就是到点。显示即规则。**

#### ⚠️ 达成截止线 = 第 (120 − 休息分钟) 分钟

休息也占环里的格子，而且必须塞得下，所以它是**环尾预留的一段**。反过来推：
**专注必须在预留段开始之前达成**，否则触底结束。

| 专注设定 | 休息 `⌈focus/5⌉` | 达成截止 | 需要的有效率 |
|---|---|---|---|
| 10 分钟 | 2 | 第 118 分钟 | ≥ 8.5% |
| 25 分钟 | 5 | 第 115 分钟（1:55） | ≥ 21.7% |
| 50 分钟 | 10 | 第 110 分钟（1:50） | ≥ 45.5% |

⚠️ 这条推论的价值在于：**「达成太晚、休息被截断」这个边界情况不是被解决了，
是被设计掉了**——休息永远塞得下，因为它的位置就是从环尾倒推的。

⚠️ 难度曲线是自然长出来的，不是调出来的：选 50 分钟就得有效率过半。
**这是用户自己选的，程序不解释、不提醒、不放宽。**

### 4.4 阶段与按钮

| 阶段 | 按钮 | 环上画什么 |
|---|---|---|
| 空闲 | **Start**（选目标、调分钟） | — |
| *（终结之后就是空闲）* | | *⚠️ 见下* |
| 专注中 | **Give up** | 按秒数高度的绿 / 红 / 空白 |
| 休息中 | **Give up**（⚠️ **不变成 Start**） | 淡蓝（见 §5） |

⚠️ 休息期间按钮**不变成 Start**——不给「一口气连干三轮」的捷径。但用户执意
Give up 再开新轮，**尊重选择，不拦**。这跟产品克制是一致的：它不拦你，只是不给你
那个按钮。

⚠️ **一轮终结（达成 / Give up / 触底 / 关窗）之后就是空闲，所以环要清空**
——2026-09-16 实机跑完一轮时发现漏了这条，用户问「圆环没消失，故意的吗」。
不是故意的。**留着它是有害的**：真的分针还在走，而环冻在起点那一圈上，扫一眼
极容易读成「现在正在走这圈」。成绩留在文字上（`Done. 10 min of focus.` + 累计）
就够了——空盘是下一轮的邀请。

### 4.5 落盘：这不是政策，是一个动作

**屏幕上显示的那个数，就是要写进文件的数。** 任何一次受控退出都执行同一个写入动作：

```
达成目标 ──┐
Give up   ──┤
环触底    ──┼──▶  加进库的 total 表（一轮一次）
关闭窗口  ──┤
Cmd+Q     ──┘

程序崩溃  ──▶  没办法，本轮丢失（已存历史不受影响，文件根本没被打开过）
```

文件简化成一行语义。v3 的 `recordedThrough` checkpoint **不再有存在理由**
（没有回填，就不需要水位线）：

```json
{ "goals": { "编程": { "seconds": 57203 } } }
```

**显示** = `文件里的总数 + 本轮实时累计`，贴在**每个目标自己那一行**右边，
格式是**小时、两位小数、不带单位**（`15.89`，跟 v3 一致）。账本里存的是整数秒
（C7：1 Hz 采样，更细是假精度），显示时才 `/ 3600.0`——⚠️ 写成 `3600` 的话整数除法
会把小数位悄悄吃掉。**每分钟刷新**
——格子每分钟才封盘，秒级跳动是噪声。

⚠️ **知情代价**：崩在第 119 分钟，那 119 分钟全没了。v3 至少还能靠 `Backfill`
从 AW 把历史捞回来，v4 没有这个后路。这是「交出客观审计」（DECISIONS A1）那笔
交易的延伸。

### 4.6 余量就是整件事的算术（2026-09-16 写引擎时落定）

§4.3 的截止线和 §5 的「淡蓝尾撞到环终点」是**同一个减法的两种读法**：

```
预算 Budget  = (120 − 休息分钟) × 60 − 专注目标秒      ← 整轮总共能浪费多少秒
浪费 Wasted  = 已经过去的秒 − 已攒下的专注秒
余量 Slack   = Budget − Wasted                         ← 淡蓝尾 → 环终点
触底         ⟺ Slack < 0
```

`RingSeconds − 淡蓝尾 == Slack` 这个恒等式有测试钉着（`RingTests`）。
它的用处是：**渲染层不需要再推一遍规则**，三段色块的位置直接从这里出。

⚠️ **跑偏的秒和没采到的秒一起算进 Wasted，这跟 §4.1 不矛盾**：
「没采到的秒既不计入也不算跑偏」说的是**不冤枉人**（不画红格、不记在谁头上），
可它照样从环上过去了，照样吃余量。**「睡一觉刷满番茄钟」做不到，靠的正是这两件事是分开的。**

#### 一秒的四种结果（2026-09-16 起真的只有四种：`SelfExempt` 已删，见 DECISIONS C9）

| 结果 | 进 `sampledSeconds` | 进 `focusedSeconds` | 什么时候 |
|---|---|---|---|
| `Focused` | ✅ | ✅ | 命中某个勾选的目标 |
| `OffTask` | ✅ | ❌ | 采到了，不命中 |
| `Away` | ❌ | ❌ | 键鼠空闲 ≥ 180 秒，人不在（C13）。**压过下面一个，也压过屏幕上是什么** |
| `Unread` | ❌ | ❌ | 连 app 名都没读到：平台层失败、Windows 锁屏（C10） |

⚠️ **前台是 ItamiBen 自己没有任何特殊待遇**——它就是普通的 `OffTask`（C9，2026-09-16
用户拍板删掉硬编码豁免）。想让它算专注，去库的 `rule` 表写一条规则。

⚠️ `Away` / `Unread` 分成两个取值，一半为了**日志里能看出「这一秒为什么没算」**，
一半因为格子上画法不同（`Away` 画空心虚线框，`Unread` 什么都不画）；在**计数**层面
它们等价，**别在环的逻辑里给它们分叉**。

⚠️ **`Unread` 盖不住 macOS 的锁屏**：2026-09-16 实测，macOS 锁屏时前台读到的是
`loginwindow` / `Login`——**是个正常的 app 名**，所以只能靠 `Away` 挡。
（Windows 那边 `GetForegroundWindow()` 返回 0，走 `Unread`。两个平台锁屏的表现不一样。）

#### 专注长度的结构上限

`focus + ⌈focus/5⌉ ≤ 120` 解出来是 **100 分钟**——这是「理论上还有可能完成」的边界，
不是推荐值。界面给的档位是 10 / 25 / 50（C6：别往上加档）。

## 5. 钟面视觉：淡蓝休息块与三段读数

休息**不画扇形**，就是环上一段**淡蓝色**，位置紧跟在预计达成点之后。于是环上从分针
往前看是三段连续色块：

```
分针 ──[灰色 承诺弧]──[淡蓝 休息块]────空白────▶ 环终点
       ↑还要专注多久     ↑休息多久       ↑还剩多少余量
```

| 读数 | 含义 |
|---|---|
| 分针 → 淡蓝起点 | **还需要专注多久** |
| 淡蓝块长度 | 休息多久 |
| 淡蓝块尾 → 环终点 | **你还剩多少余量** |
| 淡蓝块尾**撞到**环终点 | 结束 |

**截止线不需要画——环的终点本来就是它。** 而「余量」这一段在 v3 里压根不存在，
因为 v3 没有硬预算。

⚠️ **这块蓝随着你摸鱼一格一格往环尾退，这就是惩罚本身。** 不弹窗、不出声，
只是看着它退。产品名 Itami =「一袋米要扛几楼」= **痛みを感じろ** 的空耳——
名字、核心视觉、惩罚机制，在这里是同一件事。

### 5.1 预告与已发生要分开

休息块在分针走到之前是**预告**，走进去之后是**正在发生**。

- **预告**：淡一档 / 描边
- **已发生**：填实

⚠️ 不分开的话，分针走进休息区时画面**毫无变化**，用户无法确认休息到底开始没有。
v3 对承诺弧有同样的区分（`Gray` 表示未发生的未来）。

### 5.2 灰色承诺弧保留

它填满「分针 → 淡蓝起点」那一段。⚠️ **不能省**：省了之后那段是空白，跟「余量」
那段的空白**长得一模一样**，两个完全不同的含义会撞色。

### 5.3 暗色主题下淡蓝要单独调

饱和色在两个模式下观感差很多（v3 的 M 组踩过）。淡蓝在深底上容易发灰发脏，
大概率要给 dark 单独调一档，跟 v3 处理提示条底层那样。

## 6. 可以从 v3 直接搬的

跟 AW 无关、已经真机验证过多轮的部分。搬之前去 `../ItamiTimer/DECISIONS.md` 查对应
条目——那里记着每一条的代价和「不要做成」。

| 项 | 状态 |
|---|---|
| 钟面渲染规格（几何 + 材质与光四层） | ✅ 2026-09-16 搬完，休息扇形换成淡蓝块 |
| 骨牌（七个 = 星期几，纯矢量） | ✅ 2026-09-16 搬完，原样不动 |
| 界面布局（绿红按钮 / 无数字滑块 / 目标行 / 版权行） | ✅ 2026-09-16 搬完，见 DECISIONS C14 |
| 主题 Light / Dark | ✅ 搬完，淡蓝单独调了一档（D4） |
| 闹钟（滚轮拨针 + 黄针 + 一次性 + 加速） | ✅ 2026-09-16 搬完，见 §8 |
| 声音（只用系统音 + 连响 + 读文件头算间隔） | ✅ 2026-09-16 搬完 |
| 提醒清单 | ✅ 2026-09-16 搬完，见 §10 |
| 透明度（`setting.opacityPercent`）、无边框置顶窗口 | ✅ 2026-09-16 搬完，见 §11 |
| 承诺弧 / 截止弧 / 归档的判定容器 | ⛔ **不搬**：v4 的两小时硬环取代了它，归档整套不存在（DECISIONS C4） |
| 三声通知（达成 / 休息完 / 飘了） | ✅ 2026-09-16 搬完，各响 2 遍（E4 已推翻当初不搬的理由） |
| 确认框、小红圈点击、设置窗口 | ✅ 2026-09-16 搬完 |

## 7. App 层是怎么接上去的（2026-09-16 落地，实机验过）

```
Sampler（100ms 一拍）
  ├─ UI 线程：ForegroundWindow.ReadApp()      微秒级、零权限、永远拿得到
  └─ 后台线程：ForegroundWindow.ReadTitle(h)  带 250ms 超时，尽力而为
        ↑ 只读 UI 线程递过来的句柄，自己不碰 AppKit（DECISIONS B6）
        ↓ 回来的标题**带着它是从哪个 app 读的**
  → 两边 app 对不上就当标题未知（B2）
  → Round.Observe(now, app, title)            「这一秒采过没有」的哨兵在这里
  → 画 Round.Cells + Round.Project()
```

⚠️ **一秒十拍，不是一秒一拍**（§2.1.1 实测：`Interval=1s` 13 分钟漏 5 拍且不报错）。
去重靠 `Round` 的哨兵，所以多采无害、漏采才要命——**宁可多跑九拍**。

### 7.1 实机验收（2026-09-16 03:31~03:33，M1 Mac mini）

| 验的东西 | 结果 |
|---|---|
| 一秒一行、不重不漏 | ✅ 日志里 `focused` 每秒 +1，连续 42 秒无跳号 |
| 25 分钟档的预算 | ✅ 界面「90 min of slack」= 5400 秒，跟 §4.3 的表逐字对上 |
| 10 分钟档 | ✅ `break=2min deadline=min118 budget=6480s` |
| 开头那笔账（C11） | ✅ 03:31:52 点的 Start，`slack` 从 5400 直接起于 5348——被抹掉的 52 秒确实吃了余量 |
| 跑偏 | ✅ 换成不匹配的目标后 `focused` 不动、`slack` 每秒 −1，环上出红格 |
| 三段色块 | ✅ 灰弧从分针起、淡蓝紧跟其后（描边淡填 = 预告）、后面留白 |
| 落盘（C5） | ✅ Give up 写出 `{"goals":{"编程":{"seconds":42}}}`，中文没转义，只写一次 |
| ~~**自身豁免（C9）**~~ ⚠️ **这条验的是已删掉的行为**（2026-09-16 晚些时候用户拍板删了特例，见 C9），保留是因为它同时验证了 `localizedName` 返回 `ItamiBen` 这个事实 | ✅ **两半都对**：前台切到 ItamiBen 自己的 22 秒里，`focused` 一秒没涨（盯着钟面换不来专注），`slack` 每秒 −1（但它照样吃余量）。`NSWorkspace.localizedName` 返回的就是 `ItamiBen` 这个字符串——A4 那条正则站得住 |
| ~~那 22 秒不画红格~~（同上，现在**会**画红格） | ✅ 它们没进 `sampledSeconds`，所以 `OffTaskSeconds = Sampled − Focused` 结构上就是 0；那一格的红柱比满格矮一截 |
| Give up 时本轮 0 秒 | ✅ 累计原样不动（`Add` 对 0 秒是空操作），没有写出一条 0 秒的空记录 |

## 8. 闹钟与声音（2026-09-16 从 v3 搬过来）

模型在 `Core/AlarmClock.cs`，**状态只有一个值**：响铃时刻。黄针位置是它对 12 小时
取余的推导值——存两份会漂（v3 的 E7）。

```
滚轮拨针 → Bump(分钟, now) → FireAt = NextRing(now, 新位置)   ← 拨那一刻就算死
每拍     → ShouldFire(now) → MarkFired() + Sound.Repeat(音色, 4)
退出     → 写库的 setting 表一次
启动     → Restore(时刻)：只画黄针，**不激活**
```

- **三级判断全部严格小于**：恰好跟时针重合 = 12 小时后，不是「就是现在」——否则
  拨着拨着会当场响。
- **加速看的是拨的节奏，不是单次 Delta**：Avalonia 的 `Delta.Y` 一格就是 1.0，
  v3 照搬 Win32 的 `/120` 之后那道梯子**一次都没跑起来过**，而且不报错。
  现在数「连着拨了几格」，300ms 断档：1 → 3 → 8 → 15 → 30 分钟/格。
- **响 4 遍**，间隔 = 音频文件自己的长度（读文件头算，不解码不播放）。
  固定间隔不行：Windows 的 winmm 只有一个通道，下一遍早到会把上一遍从中间掐断；
  macOS 那边则是叠着响。两种都不叫「响了四遍」。

### 8.1 实机验收（2026-09-16 03:41~03:44）

| 验的东西 | 结果 |
|---|---|
| 黄针 + 滚轮 | ✅ 拨针跟标签同步，方向跟 v3 一致（前滚逆时针） |
| 到点响 | ✅ `03:44:00` 整点，`alarm fired: 03:44 sound=Sosumi ×4`，**日志零 WARN / ERROR**（音频文件找到了、SoundID 建成了、播放没抛） |
| 响过即撤 | ✅ 标签变成 `⏰ 03:44 · off`，黄针留在原地 |
| 不补响 | ✅ 退出重开，恢复的时刻已经过点，**没有第二次 `alarm fired`** |
| 退出落盘 | ✅ `{"alarmSound":"Sosumi","alarmAt":"2026-09-16T03:44:00+08:00"}` |

## 9. 观测库：自己当 ActivityWatch（2026-09-16 定）

### 9.1 为什么是这个形状

v4 原来的做法是「采样直接喂判定引擎」，一秒一拍当场定死。它没错，但**交出去的东西
比必要的多**：程序一关，本轮就没了（原来的 C5 明写着「崩在第 119 分钟，那 119 分钟
全没了」）。

换成「一秒一行写进 SQLite，判定时整段读回来重建」之后：

- **重建是幂等的**——`Round.Observe` 的哨兵只进不退，同一段重放多少次结果都一样。
  「任何时候根据库里的内容重建 120 分钟的环，结果都一样」这条性质是白捡的。
- **崩溃不再丢本轮**——观测已经在库里了。
- v3 那套判定和钟面**长在这个数据形状上**，所以能直接用，不用重写。

⚠️ 但**真相源仍然只有一个**：本程序自己读的前台窗口。aw-server 不在链路里，
回来的只是数据的形状（DECISIONS A1 改写版）。

### 9.2 表结构

```sql
CREATE TABLE app   (id INTEGER PRIMARY KEY, name TEXT NOT NULL UNIQUE);
CREATE TABLE title (id INTEGER PRIMARY KEY, text TEXT NOT NULL UNIQUE);
CREATE TABLE sample (
  at       INTEGER PRIMARY KEY,          -- Unix 秒 UTC；rowid 即索引
  app_id   INTEGER REFERENCES app(id),   -- NULL = 当时读不到前台窗口
  title_id INTEGER REFERENCES title(id),
  idle     INTEGER NOT NULL              -- 距上次键鼠输入的秒数，**原始值**
);
```

三条承重的理由在 DECISIONS F1~F3，这里只记数：一轮两小时 7200 行，去重之后整库约
150KB；盯着一个标题十分钟是 600 行 `sample` 但只有 1 行 `title`（实测标题 67 字节，
不去重就是 40KB）。

### 9.3 一条线索，不是两条

AW 那边窗口和 afk 是两个 watcher、两个桶、两套写入节奏，而**它们节奏不同正是 v3
最后那个大 bug 的根因**：窗口桶每 10 秒必跳、afk 桶能 35 秒不动，各自判断各自的
`last_updated`，于是「这一拍没取到 afk」就没人把红格盖回来，锁屏两小时画成一片红。

这里一秒一行、一次写完，那个失败模式**结构上不存在**。

### 9.4 ⚠️ 顺带纠正 v3 记的 Note T1

v3 的 `AwClient` 里写着「AW 只按事件开始时刻过滤，跨进区间的事件会凭空消失」，
并为此建了一整套「先问谁跨进来」的补偿机制。**2026-09-16 对本机 aw-server 实测，
这条不成立**：

```
查 19:50:00~19:51:00（完全落在一条 346 秒的事件内部）
→ timestamp=19:50:00  duration=60.001     ← 相交过滤 + 两头裁到区间里
```

对我们没有直接影响（那一层整个不搬，F5），记在这里只是因为**任何时候拿 v3 的注释
当事实之前，都要先量一遍**——这个项目已经因为「未实证的推理支撑架构决策」栽过一次
（DECISIONS B5）。

### 9.5 重建：一条链，两个入口

```
SampleStore.Read(起点, 此刻)        ← 库里真的有记录的那些秒
  → AwayMap.Of(rows)               ← 跨行算离开区间（起点回到 at − idle + 1）
  → Round.Observe(每一行, away)     ← 哨兵只进不退 ⇒ 重放幂等
  → Round.Advance(此刻)            ← 补最后一段：库里最后一行到现在之间可能什么都没有
```

**两个入口，同一条链：**

| 入口 | 什么时候 | 准确度 |
|---|---|---|
| 实时 | 每写一行就顺手喂一次环 | 读数是秒级实时的，但**离开只能按当前这一秒判** |
| 重建 | 每分钟一次（以及崩溃恢复） | **权威**：跨行回溯，把跨门槛之前那 180 秒纠正成空白 |

⚠️ 所以「刚才还是红的变成空白」是**语义不是 bug**——v3 对同一件事有同样的行为。
最多红 180 秒，下一次整分钟重建就纠正了。

⚠️ 每分钟重建时顺带对一次账，但**「对不上」有两种完全不同的原因，不能混成一条警告**
（2026-09-16 实机第一次跨门槛就撞到了）：

| 现象 | 原因 | 该打什么 |
|---|---|---|
| 离开区间**新增**了一条，同时对不上 | 跨过 180 秒门槛，之前那最多 179 秒被追认成离开——**设计本身** | `retroactive away`（普通日志） |
| 区间条数没变却对不上 | 真的有秒没写进库 | `rebuild mismatch`（WARN） |

头一版把两者都打成「有秒没写进库」，实机报了一条假警告（live=532 → db=353，正好
179 秒）。**一个会说谎的自检比没有自检更糟**——它会训练你忽略它。

⚠️ 恢复那条路**不对账**：那时手里的环是刚构造出来的空壳，跟重建结果必然不同。

## 10. 提醒清单（2026-09-16 从 v3 搬过来；2026-09-16 晚从 alarms.cron 搬进库，I15）

一份**标准 crontab**，严格照 Linux（Vixie/cronie）语义，一条方言都不加。
`Cron.cs` / `AlarmsList.cs` 从 v3 原样搬（跟 AW 无关），护栏在
`../ItamiTimer/DECISIONS.md` 的 J 组，那些条目原样有效。

```
<运行时目录>/schedule.md   人和 AI 写；程序每分钟比一次文件 mtime，改完不用重启
                           <5 个标准 crontab 字段>  <提醒文字>  [!命令名]
```

⚠️ 一路是 `alarms.cron` → 库的 schedule 表（I15）→ `schedule.md`（I30）。
**多出来的能力是行尾那个 `!命令名`**：一条计划可以跑 `commands.md` 里的命令，
而不只是提醒。⚠️ **提醒文字必填**——它让「机器自己做了事却没说为什么」写不出来。

几条要记住的：

- **日/周两栏都不以 `*` 开头时是「或」**（`0 9 1 * MON` = 每月 1 号**或**每周一）。
  这是 crontab(5) 明文写着的行为，判据是**字段文本是不是以 `*` 开头**（`*/2` 也算），
  不是「掩码是不是全 1」。⚠️ 在一个号称「通用格式」的东西上偷偷改语义，比这个坑本身更坏。
- **第六列永远只是文字，永远不会被执行**（v3 的 J17）。
- **读不懂的行跳过，但要记一笔**（DECISIONS I29 修订了 J16 的「安静跳过」）：原因写进
  `error.log`。响了也在 `event.log` 里留一行、**带命中的表达式原文**——多条规则时
  不带它就只知道响过、不知道是哪一行响的。
  ⚠️ 护栏只覆盖一半：程序报得了「这行读不懂」，报不了「`!sleap` 这个名字不存在」。
- **水位线是纯内存的，初始化成启动那一刻**（J7）：关机期间错过的不补响。
- **视野 12 小时**：钟面小红圈只标 12 小时以内的下一条。在这个窗口里 mod-12 是**双射**，
  所以盘面上两条重叠**当且仅当它们真的在同一分钟**——那一种由「外圈变橙 + 中心补一点」
  两个独立信号表示。

### 10.1 整分钟那一串的顺序（定死的）

```
① 提示条到期收起   ⚠️ 必须在②之前，反过来会把②刚画上的当场擦掉
② 计划表到点（响 2 遍 + 提示条一分钟 + 事件）
③ 小红圈位置重算   每拍整个重算，不存在「清除上一次画的圆」
──────────────────
④ 闹钟（响 4 遍）  ⚠️ 排在最后：winmm 单通道下后响的掐断先响的，
                     而闹钟响完什么都不留、清单响完还留着提示条 ⇒ 让闹钟赢
```

### 9.6 实机验收（2026-09-16 09:47~10:23，完整一轮 10 分钟档）

| 验的东西 | 结果 |
|---|---|
| 一秒一行、不重不漏 | ✅ `slack` 每秒 −1，2035 行无跳号 |
| 去重 | ✅ 2035 行采样只占 **4 个 app + 14 个标题**，整库 60KB |
| 每分钟重建 | ✅ 每个整分触发，`rows` 逐分钟 +60 |
| **离开的回溯纠正** | ✅ 10:00 起 `away=1`、10:15 起 `away=2`；跨门槛那一次把 **179 秒**从专注改判成离开 |
| 账能对上 | ✅ 实时日志 779 条 `Focused` − 179 条追认 = **600**，正好是 10 分钟档的目标 |
| 崩溃恢复 | ✅ `kill -9` 之后整轮回来，界面一并还原；停机那 4 秒**吃了余量但没判跑偏** |
| 落盘 | ✅ `during.json` 642 = 42（前一次）+ 600；`round` 表 `Completed` |
| 环上的缺口 | ✅ 09:48 缺第 19~22 秒 = `kill -9` 那 4 秒；09:47 缺 1 秒 = Start 抹到整分那一秒（C11） |

## 11. 窗口外观（2026-09-16 从 v3 搬过来）

无边框 + 透明底 + 置顶。**整扇主窗口的属性，不是另开一扇挂件**——功能一个不变，
只是外框和窗口底色没了。护栏在 DECISIONS G 组。

```
<运行时目录>/layout.md，两个键，**只在启动时读一次**（改完必须重启）
  Layout          "standard" | "compact"    standard 380px 宽 / compact 292px
  OpacityPercent  10~100                    写错一律强制 90（不夹边界）
```

⚠️ 一路是 `layout.json` → rules.json（I14）→ 库（I15）→ `layout.md`（I30）。
⚠️ **「只在启动时读一次」是这两个键独有的**，别推广到别的配置上：目标 / 规则 /
命令 / 计划都是每分钟重装、当场生效。

没有标题栏之后，两件事换了入口：**拖窗口靠钟面（左键）、关窗口靠钟面右键菜单**。
菜单只有「Keep on top」和「Close window」两项。

右上角两个小图标（跟左上角的闹钟时刻对称）：

| 图标 | 状态怎么读 |
|---|---|
| 图钉 | **填实 + 亮** = 置顶中；**空心 + 淡** = 没置顶 |
| 主题 | 太阳 = 日面，月亮（被咬一口的饼干）= 夜面。**画的是当前状态，不是点了会变成什么** |

⚠️ 图钉那个状态有**四种表现**（图标 / 右键菜单那一项 / `settings.pinned` / `Topmost`），
只有 `SetPinned` 一个入口——分家之后界面还是好好的，只是说的不是同一件事。

### 11.1 ⚠️ 不透明度必须用 `OpacityMask`

Avalonia 的 `Visual.Opacity` 是**逐个绘制操作**各自半透，不是「整个控件先合成一层
再降透」。钟面那块白是画在木色**实心圆盘**之上的，逐笔半透会让木色透过白面混上来，
出来是米黄——v3 的用户一眼就看出「表盘泛黄」。

headless 实测：`Opacity=0.5` 时白面净区是 `(207,189,176, A=201)`，
`OpacityMask=0.5` 是 `(255,255,255, A=127)`。

2026-09-16 在 ItamiBen 上复验：opacity 35% 时钟面明显变淡而**颜色不偏**。
