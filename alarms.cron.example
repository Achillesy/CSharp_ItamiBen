# ItamiBen 的提醒清单。把这个文件拷到运行时目录并改名 alarms.cron：
#   macOS    ~/Library/Application Support/ItamiBen/alarms.cron
#   Windows  %LOCALAPPDATA%\ItamiBen\alarms.cron
#
# **这是一份标准 crontab**，严格照 Linux（Vixie/cronie）语义，一条方言都不加。
# 前五个字段是 分 时 日 月 周，**剩下的到行尾全是提醒文字**。
#
#   *        每一个
#   5        就是 5
#   1-5      1 到 5
#   1,3,5    1、3、5
#   */15     从下限起每 15
#   5/15     5 到上限，每 15
#   MON JAN  星期和月份认三字母名，大小写不敏感；星期的 0 和 7 都是周日
#   @daily @hourly @weekly @monthly @yearly   照 crontab(5) 展开
#
# ⚠️ **日/周两栏都不以 * 开头时是「或」**，这是 crontab(5) 明文写着的行为：
#    `0 9 1 * MON` = 每月 1 号**或**每周一。只要有一栏以 * 开头就是「与」。
#
# ⚠️ **第六列永远只是文字，永远不会被执行。**
#
# ⚠️ **不做任何语法检查**：写错的行安静跳过，屏幕上和日志里都零反馈
#    （写错了在 Linux 下也一样错）。响了才会在 itamiben.log 里留一行，带表达式原文。
#    所以「提醒没响」的诊断方式是：翻日志查不到记录 ⇒ 反推自己写错了。
#
# ⚠️ 视野是 **12 小时**：钟面上那个小红圈只标 12 小时以内的下一条。
#    更远的安排程序照样会响，只是钟面上看不到。
#
# 「这条暂时不响」就是 crontab 自己的注释语法：行首加 #。

0 9 * * MON-FRI     该动一动了
30 12 * * *         吃饭
0 14,16 * * *       喝水
@daily              今天的复盘
55 23 1 * *         月度对账
